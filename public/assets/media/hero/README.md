# BBF Hero Media

Assets del hero principal del sitio.

**Reservado para migración M5-D3** (HeroVideo molecule refactor).

Actualmente los assets viven en `public/media/hero/`:
- `hero.av1.webm` — canon AV1 (~80% navegadores)
- `hero.h264.mp4` — fallback H.264 (~99% navegadores)
- `hero-poster.jpg` — poster frame

En M5-D3 se evaluará migración a esta ubicación canónica.
