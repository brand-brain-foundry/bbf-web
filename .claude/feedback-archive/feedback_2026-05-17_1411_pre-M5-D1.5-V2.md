# COPY_PASTE_B-BBF-WEB-M5-D1.5-LOGO-SYSTEM-V2.md

**Despacho:** B-BBF-WEB-M5-D1.5-LOGO-SYSTEM-V2 (consolidado)
**Para:** Claude Code en VS Code
**Repo:** `/Volumes/PK/BBF/Repos/bbf-web/`
**Branch:** `feat/m5-design-system`
**HEAD pre:** `ac0dbf9`
**Tiempo estimado:** 2-2.5h
**STOPs:** 12

---

## §0 — INSTRUCCIONES OPERATIVAS

### §0.1 Acciones autónomas previas

ANTES de comenzar, ejecutar:

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

# Archivar feedback anterior si existe
mkdir -p .claude/feedback-archive
[ -f feedback.md ] && mv feedback.md .claude/feedback-archive/feedback_$(date +%Y-%m-%d_%H%M)_pre-M5-D1.5-V2.md

# Verificar branch + HEAD
git branch --show-current
git log --oneline -3
```

### §0.2 Disciplina canon

Mismas que sub-módulos anteriores. STOPs respetados. Reportes literales. NO improvisar fuera scope. Si encuentra ambigüedad, **REPORTAR Y DETENER** — Strategic emite ajuste.

### §0.3 Cambios visuales esperados

**M5-D1.5 NO debe cambiar el hero visualmente.** El hero usa logo en variante stamp animada. Después del despacho, el hero debe verse **IDÉNTICO**.

### §0.4 Decisiones firmadas que rigen

| ID | Aplicación |
|----|------------|
| D-BBF-WEB-77 | Surface-awareness (color via currentColor) |
| D-BBF-WEB-78 | Animated state (CSS-driven via data-attribute) |
| D-BBF-WEB-80 | Slot asChild (compatible, no implementado en M5-D1.5 logo) |
| D-BBF-WEB-81 | Folder per component (atoms/BBFLogo/ existe) |
| D-BBF-WEB-82 | AI-readable (JSDoc + data-variant + READMEs assets) |
| D-BBF-WEB-83 | Atomic Design folder structure |
| D-BBF-WEB-84 | **NUEVO** — Estructura `public/assets/` canon BBF (opción A completa) |

---

## §1 — OBJETIVO

1. **Establecer estructura canónica `public/assets/`** BBF (D-84)
2. **Migrar SVGs `/tmp/` → `public/assets/brand/logos/`**
3. **Mover `BBF-Logo-Stamp.svg` legacy** a nueva ubicación
4. **Eliminar dirs obsoletos** (`/tmp/`, `/public/logos/`)
5. **Implementar 4 variantes Logo canon** (icon/horizontal/name-only/stamp)
6. **Update BBFLogo.tsx + .variants.ts**
7. **Crear `logo.css` canon component**
8. **Update `hero.css`** (remover reglas logo legacy)
9. **Update `page.tsx`** uso nueva API

---

## §2 — STOP 1: PRE-EXECUTION AUDIT

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "=== Branch + HEAD ==="
git branch --show-current
git log --oneline -3
# Esperado: ac0dbf9

echo ""
echo "=== tsc ==="
pnpm typecheck 2>&1 | tail -3

echo ""
echo "=== /tmp/ contenido ==="
ls -la tmp/ 2>/dev/null || echo "tmp/ no existe"

echo ""
echo "=== public/ contenido raíz ==="
ls -la public/ | head -20

echo ""
echo "=== public/logos/ contenido ==="
ls -la public/logos/ 2>/dev/null

echo ""
echo "=== Hero video assets actuales (path ubicación) ==="
find public/ -type f \( -name "*.webm" -o -name "*.mp4" -o -name "hero*" \) 2>/dev/null | head -10

echo ""
echo "=== OG images actuales ==="
find public/ -type f \( -name "og-*" -o -name "opengraph*" -o -name "twitter*" \) 2>/dev/null | head -10

echo ""
echo "=== BBFLogo.tsx path actual ==="
grep -n "BBF-Logo-Stamp\|/logos/\|/assets/" src/components/atoms/BBFLogo/BBFLogo.tsx | head -5

echo ""
echo "=== HeroVideo.tsx path actual ==="
grep -n "/hero\|src=" src/components/HeroVideo.tsx 2>/dev/null | head -10
```

**Reporte esperado:** estado actual del filesystem + paths actuales en código.

---

## §3 — STOP 2: AUDIT SVG LITERAL `/tmp/`

```bash
echo ""
echo "═══ SVG LITERAL DUMPS — STRATEGIC REVIEW REQUIRED ═══"
echo ""
echo "--- BBF_Logo-Icon.svg ---"
cat tmp/BBF_Logo-Icon.svg
echo ""
echo "--- BBF_Logo-Name-H.svg ---"
cat tmp/BBF_Logo-Name-H.svg
echo ""
echo "--- BBF_vLogo-Name-Circle.svg ---"
cat tmp/BBF_vLogo-Name-Circle.svg 2>/dev/null || cat tmp/BBF_Logo-Name-Circle.svg
echo ""
echo "--- public/logos/BBF-Logo-Stamp.svg (LEGACY reference) ---"
cat public/logos/BBF-Logo-Stamp.svg
```

### §3.1 ⏸️ PAUSA OBLIGATORIA

**Después del dump, DETENER ejecución.** Strategic necesita ver el contenido literal de los 4 SVGs (3 nuevos + 1 legacy) para:
- Confirmar IDs preservables para animación
- Decidir optimización exacta por SVG
- Detectar inconsistencias viewBox/dimensions
- Emitir versiones optimizadas exactas

**Estado:** STOP 2 reporta → **ESPERA Strategic** → recibe ajuste → STOP 3+

**No proceder con STOPs 3-12 hasta Strategic emita las versiones SVG optimizadas.**

---

## §4 — STOP 3: CREAR ESTRUCTURA `public/assets/` CANON

⚠️ **Strategic emitirá este STOP DESPUÉS del audit STOP 2.**

Pero estructura general a crear (canon firmado D-84):

```bash
mkdir -p public/assets/brand/logos
mkdir -p public/assets/brand/icons
mkdir -p public/assets/brand/patterns
mkdir -p public/assets/brand/guidelines
mkdir -p public/assets/media/hero
mkdir -p public/assets/media/images/team
mkdir -p public/assets/media/images/cases
mkdir -p public/assets/media/images/og
mkdir -p public/assets/media/videos
mkdir -p public/assets/media/audio
mkdir -p public/assets/illustrations
mkdir -p public/assets/fonts
mkdir -p public/assets/documents

# .gitkeep en dirs reservados M6+ (para que git los preserve vacíos)
touch public/assets/brand/icons/.gitkeep
touch public/assets/brand/patterns/.gitkeep
touch public/assets/brand/guidelines/.gitkeep
touch public/assets/media/images/team/.gitkeep
touch public/assets/media/images/cases/.gitkeep
touch public/assets/media/videos/.gitkeep
touch public/assets/media/audio/.gitkeep
touch public/assets/illustrations/.gitkeep
touch public/assets/fonts/.gitkeep
touch public/assets/documents/.gitkeep
```

**Strategic emitirá contenido literal de READMEs para subdirs principales.**

---

## §5 — STOPs 4-11 (Strategic emite post-STOP 2)

| STOP | Acción |
|------|--------|
| 4 | Crear SVGs optimizados en `public/assets/brand/logos/` (Strategic emite contenido) |
| 5 | Mover `BBF-Logo-Stamp.svg` legacy + mover hero/og assets si encuentra |
| 6 | Eliminar `/tmp/` y `/public/logos/` (vacíos) |
| 7 | Crear READMEs principales (Strategic emite) |
| 8 | Refactor `BBFLogo.variants.ts` con 4 variants |
| 9 | Refactor `BBFLogo.tsx` con loadSvg + paths nuevos |
| 10 | Crear `logo.css` canon + cleanup `hero.css` |
| 11 | Update `page.tsx` + build verde |

---

## §6 — STOP 12: COMMIT + PUSH + REPORTE

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

git status --short

# Add SELECTIVO
git add public/assets/
git add src/components/atoms/BBFLogo/
git add src/styles/tokens/components/logo.css
git add src/styles/tokens/components/hero.css
git add src/app/globals.css
git add src/app/\(frontend\)/\[locale\]/page.tsx

# Solo si HeroVideo.tsx tocó paths:
git add src/components/HeroVideo.tsx 2>/dev/null

# Mover legacy con git mv si aún no se hizo
# (Strategic emitirá comandos específicos en STOP 5)

git status --short

git commit -m "feat(m5-d1.5): logo system canon + public/assets/ structure

B-BBF-WEB-M5-D1.5-LOGO-SYSTEM-V2 (consolidado)

Establece estructura canónica de assets BBF + implementa 4 variantes
del atom Logo. Preserva visual hero idéntico.

Decisión firmada nueva:
- D-BBF-WEB-84: estructura public/assets/ canon BBF (opción A completa)

Decisiones aplicadas:
- D-BBF-WEB-77: surface-aware (color via currentColor)
- D-BBF-WEB-78: animated state (CSS-driven)
- D-BBF-WEB-82: AI-readable (READMEs + JSDoc + data-attributes)

Cambios:
- public/assets/ estructura canon creada
- SVGs nuevos optimizados en public/assets/brand/logos/
- BBF-Logo-Stamp.svg movido a public/assets/brand/logos/ (legacy preservado)
- /tmp/ eliminado, /public/logos/ eliminado
- READMEs en subdirs principales
- BBFLogo atom: 4 variants (icon/horizontal/name-only/stamp)
- logo.css canon component creado
- hero.css limpiado (reglas logo movidas)
- page.tsx uso: <BBFLogo variant=\"stamp\" size=\"hero\" animated />

Verificación:
- tsc --noEmit: exit 0
- pnpm build: exit 0 (5/5 páginas)
- Visual hero idéntico

Refs: D-BBF-WEB-84, BBF_M5_D_Plan.md §2.D1.5"

git push origin feat/m5-design-system
git log --oneline -5
```

---

## §7 — REPORTE FINAL ESPERADO

```
═══ M5-D1.5-V2 COMPLETADO ═══

Branch: feat/m5-design-system
Commit: <hash>
Push: ✓

Estructura public/assets/ canon: ✓
SVGs migrados de /tmp/: ✓
Logo legacy movido: ✓
/tmp/ + /public/logos/ eliminados: ✓
4 variantes BBFLogo activas: ✓
logo.css canon: ✓
hero.css limpiado: ✓
page.tsx actualizado: ✓

Build: tsc + build exit 0 ✓
Visual hero: idéntico esperado ✓

LISTO STRATEGIC VERIFICACIÓN VISUAL VERCEL.
```

---

## §8 — METADATA

- **Despacho:** B-BBF-WEB-M5-D1.5-LOGO-SYSTEM-V2
- **Fecha:** 2026-05-17
- **HEAD pre:** `ac0dbf9`
- **Decisiones:** D-77, D-78, D-82, **D-84 NUEVA**
- **PAUSA crítica:** STOP 2 (Strategic review SVGs literal)
- **TDs heredadas:** TD-M5-D1-01, 02, 03, 04, 05, 06
- **Próximo:** Strategic emite STOPs 3-11 post-audit STOP 2

---

*B-BBF-WEB-M5-D1.5-V2 — Logo system canon + public/assets/ canon*