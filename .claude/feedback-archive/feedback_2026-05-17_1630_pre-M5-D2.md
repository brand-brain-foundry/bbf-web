# COPY_PASTE_B-BBF-WEB-M5-D2-LOCALESWITCHER.md

**Despacho:** B-BBF-WEB-M5-D2-LOCALESWITCHER
**Tipo:** Refactor M5-D2 — LocaleSwitcher molecule canon
**Para:** Claude Code en VS Code
**Repo:** `/Volumes/PK/BBF/Repos/bbf-web/`
**Branch:** `feat/m5-design-system`
**HEAD pre:** `e97f2e3` (M5-D1.5 cerrado)
**Tiempo estimado:** 2-3h
**STOPs:** 11
**Riesgo:** Medio (preservar 100% funcionalidad M4 routing ES↔EN)

---

## §0 — INSTRUCCIONES OPERATIVAS

### §0.1 Acciones autónomas previas

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

mkdir -p .claude/feedback-archive
[ -f feedback.md ] && mv feedback.md .claude/feedback-archive/feedback_$(date +%Y-%m-%d_%H%M)_pre-M5-D2.md

git branch --show-current
git log --oneline -3
```

### §0.2 Disciplina canon

STOPs respetados. Reportes literales. NO improvisar fuera scope. NO modificar Atom otros (Button/Icon/Heading/Text/BBFLogo). NO tocar lógica M4 routing.

### §0.3 Decisiones firmadas que rigen M5-D2

| ID | Aplicación |
|----|------------|
| D-BBF-WEB-77 | Surface-awareness (Pill se adapta a contexto) |
| D-BBF-WEB-78 | State management simple (active vía router state) |
| D-BBF-WEB-80 | Slot pattern asChild (no aplica en M5-D2 — Pill ES un button) |
| D-BBF-WEB-81 | Folder per component canon |
| D-BBF-WEB-82 | AI-readable (JSDoc + data-component + data-locale) |
| D-BBF-WEB-83 | Atomic Design folder structure (molecules/) |
| **D-BBF-WEB-85** | **NUEVA — LocaleSwitcher API monolítica (NO compound)** |

### §0.4 D-79 NO aplica en M5-D2

LocaleSwitcher NO es compound complejo. Aplica A-01 SB_Law Simplicidad. Compound reservado para HeroVideo M5-D3 (Source + Overlay con contenidos distintos).

---

## §1 — OBJETIVO

Refactor `src/components/i18n/LocaleSwitcher.tsx` (flat) a **molecule canon BBF** en `src/components/molecules/LocaleSwitcher/`:

- API monolítica: `<LocaleSwitcher locales={['es','en']} />`
- Surface-aware (data-surface ancestor o prop)
- Variants CVA tipados
- data-component + data-locale attributes
- M4 routing preservado 100%
- TD-M5-D2-01 resuelta (clases concatenadas sin espacio)

---

## §2 — STOP 1: PRE-EXECUTION AUDIT

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "═══ Branch + HEAD ═══"
git branch --show-current
git log --oneline -3
# Esperado: e97f2e3

echo ""
echo "═══ tsc clean ═══"
pnpm typecheck 2>&1 | tail -3

echo ""
echo "═══ LocaleSwitcher.tsx actual ═══"
cat src/components/i18n/LocaleSwitcher.tsx

echo ""
echo "═══ locale-switcher.css actual ═══"
cat src/styles/tokens/components/locale-switcher.css

echo ""
echo "═══ Imports LocaleSwitcher en el repo ═══"
grep -rn "from '@/components/i18n/LocaleSwitcher'\|from '../i18n/LocaleSwitcher'\|from './i18n/LocaleSwitcher'" src/ 2>/dev/null

echo ""
echo "═══ page.tsx uso actual LocaleSwitcher ═══"
grep -n "LocaleSwitcher" src/app/\(frontend\)/\[locale\]/page.tsx

echo ""
echo "═══ Routing helpers M4 ═══"
ls -la src/lib/i18n/ 2>/dev/null
```

### §2.1 Reporte STOP 1

```
[STOP 1 completado]

Branch: feat/m5-design-system ✓
HEAD: e97f2e3 ✓
tsc: exit 0 ✓

Estado actual LocaleSwitcher:
  - src/components/i18n/LocaleSwitcher.tsx [X líneas]
  - src/styles/tokens/components/locale-switcher.css [X líneas]
  - Imports detectados: [lista paths]
  - Routing helpers M4: [lista]

LISTO para STOP 2.
```

---

## §3 — STOP 2: CREAR ESTRUCTURA molecule

```bash
mkdir -p src/components/molecules/LocaleSwitcher
ls -la src/components/molecules/
```

---

## §4 — STOP 3: CREAR LocaleSwitcher.variants.ts

```typescript
// src/components/molecules/LocaleSwitcher/LocaleSwitcher.variants.ts
```

**Contenido exacto:**

```typescript
/**
 * BBF Design System — LocaleSwitcher molecule variants
 *
 * Subordinado a: B-BBF-WEB-M5-D2-LOCALESWITCHER
 * Decisiones: D-77, D-78, D-82, D-85
 *
 * API monolítica canon BBF.
 * Surface-aware via data-attribute ancestor + variant prop override.
 */

import { cva, type VariantProps } from 'class-variance-authority';

/**
 * Container variants
 */
export const localeSwitcherVariants = cva(
  ['bbf-locale-switcher', 'inline-flex', 'items-center', 'gap-0'],
  {
    variants: {
      surface: {
        auto: '',     // hereda data-surface ancestor
        sand: '',     // explícito sand
        dark: '',     // explícito dark
        glass: '',    // explícito glass
      },
      size: {
        sm: 'text-xs',
        md: 'text-sm',
        lg: 'text-base',
      },
    },
    defaultVariants: {
      surface: 'auto',
      size: 'md',
    },
  }
);

/**
 * Pill variants (cada botón locale)
 */
export const localeSwitcherPillVariants = cva(
  [
    'bbf-locale-switcher__pill',
    'inline-flex items-center justify-center',
    'font-medium uppercase',
    'transition-colors',
    'cursor-pointer',
    'focus-visible:outline-none focus-visible:ring-2',
    'focus-visible:ring-[var(--bbf-color-focus-ring)] focus-visible:ring-offset-2',
    'disabled:cursor-default',
  ],
  {
    variants: {
      size: {
        sm: 'h-7 px-2.5 text-xs',
        md: 'h-8 px-3 text-sm',
        lg: 'h-10 px-4 text-base',
      },
      active: {
        true: 'bbf-locale-switcher__pill--active',
        false: '',
      },
    },
    defaultVariants: {
      size: 'md',
      active: false,
    },
  }
);

export type LocaleSwitcherVariants = VariantProps<typeof localeSwitcherVariants>;
export type LocaleSwitcherPillVariants = VariantProps<typeof localeSwitcherPillVariants>;
```

---

## §5 — STOP 4: CREAR LocaleSwitcher.tsx

```typescript
// src/components/molecules/LocaleSwitcher/LocaleSwitcher.tsx
```

**Contenido exacto:**

```typescript
/**
 * BBF Design System — LocaleSwitcher molecule
 *
 * Subordinado a: B-BBF-WEB-M5-D2-LOCALESWITCHER
 * Decisiones: D-77, D-78, D-82, D-83, D-85 (API monolítica)
 *
 * Refactor desde src/components/i18n/LocaleSwitcher.tsx flat.
 *
 * API monolítica canon BBF:
 *   <LocaleSwitcher locales={['es','en']} />
 *
 * NO compound dot notation (D-85 aplica A-01 SB_Law sobre D-79).
 *
 * M4 routing preservado 100% via next-intl router.
 */

'use client';

import { useTransition } from 'react';
import { useLocale } from 'next-intl';
import { useRouter, usePathname } from '@/lib/i18n/routing';
import { cn } from '@/lib/utils';
import {
  localeSwitcherVariants,
  localeSwitcherPillVariants,
  type LocaleSwitcherVariants,
} from './LocaleSwitcher.variants';

/**
 * Locale strings tipados canon BBF.
 * Si en M6+ se agregan locales, extender este type.
 */
export type SupportedLocale = 'es' | 'en';

/**
 * Locale display labels canon BBF.
 */
const LOCALE_LABELS: Record<SupportedLocale, string> = {
  es: 'ES',
  en: 'EN',
};

/**
 * Locale aria-labels canon BBF (bilingüe accessibility).
 */
const LOCALE_ARIA_LABELS: Record<SupportedLocale, { es: string; en: string }> = {
  es: { es: 'Cambiar a español', en: 'Switch to Spanish' },
  en: { es: 'Cambiar a inglés', en: 'Switch to English' },
};

export interface LocaleSwitcherProps extends LocaleSwitcherVariants {
  /**
   * Locales a mostrar (orden visible en UI).
   * Default: ['es', 'en']
   */
  locales?: SupportedLocale[];
  className?: string;
  /**
   * Custom aria-label para el nav container.
   */
  ariaLabel?: string;
}

/**
 * BBF LocaleSwitcher molecule — Atomic Design canon
 *
 * @description
 * Switcher de idioma canon BBF. API monolítica simple.
 * Preserva M4 routing (next-intl) sin modificar lógica.
 * Surface-aware via data-attribute ancestor.
 *
 * @example Default ES/EN
 * ```tsx
 * <LocaleSwitcher />
 * ```
 *
 * @example Surface dark explícito
 * ```tsx
 * <LocaleSwitcher surface="dark" size="lg" />
 * ```
 *
 * @example Custom locales orden
 * ```tsx
 * <LocaleSwitcher locales={['en', 'es']} />
 * ```
 *
 * @example Dentro de section con surface ancestor
 * ```tsx
 * <section data-surface="dark">
 *   <LocaleSwitcher />  // auto-detecta dark
 * </section>
 * ```
 */
export function LocaleSwitcher({
  locales = ['es', 'en'],
  surface,
  size,
  className,
  ariaLabel,
}: LocaleSwitcherProps) {
  const currentLocale = useLocale() as SupportedLocale;
  const router = useRouter();
  const pathname = usePathname();
  const [isPending, startTransition] = useTransition();

  const handleSwitch = (locale: SupportedLocale) => {
    if (locale === currentLocale || isPending) return;
    startTransition(() => {
      router.replace(pathname, { locale });
    });
  };

  return (
    <nav
      data-component="bbf-locale-switcher"
      data-surface={surface !== 'auto' ? surface : undefined}
      className={cn(localeSwitcherVariants({ surface, size }), className)}
      aria-label={ariaLabel ?? 'Language switcher'}
    >
      {locales.map((locale) => {
        const isActive = locale === currentLocale;
        return (
          <button
            key={locale}
            type="button"
            data-component="bbf-locale-switcher-pill"
            data-locale={locale}
            data-active={isActive || undefined}
            disabled={isActive || isPending}
            aria-current={isActive ? 'true' : undefined}
            aria-label={LOCALE_ARIA_LABELS[locale][currentLocale]}
            className={cn(
              localeSwitcherPillVariants({ size, active: isActive }),
            )}
            onClick={() => handleSwitch(locale)}
          >
            {LOCALE_LABELS[locale]}
          </button>
        );
      })}
    </nav>
  );
}
```

---

## §6 — STOP 5: CREAR index.ts barrel

```typescript
// src/components/molecules/LocaleSwitcher/index.ts
```

**Contenido exacto:**

```typescript
export { LocaleSwitcher } from './LocaleSwitcher';
export type { LocaleSwitcherProps, SupportedLocale } from './LocaleSwitcher';
export {
  localeSwitcherVariants,
  localeSwitcherPillVariants,
} from './LocaleSwitcher.variants';
export type {
  LocaleSwitcherVariants,
  LocaleSwitcherPillVariants,
} from './LocaleSwitcher.variants';
```

---

## §7 — STOP 6: CREAR molecules/index.ts barrel

```typescript
// src/components/molecules/index.ts
```

**Contenido exacto:**

```typescript
/**
 * BBF Design System — Molecules barrel export
 *
 * Subordinado a: BBF_M5_D_Plan.md
 * Decisión: D-BBF-WEB-83 (Atomic Design folder structure)
 */

export * from './LocaleSwitcher';
```

---

## §8 — STOP 7: REFACTOR locale-switcher.css CANON

Strategic decisión: el CSS legacy usa BEM (`.bbf-locale-switcher__btn`, `.bbf-locale-switcher__btn--active`). Refactor a clases canon (`.bbf-locale-switcher__pill`, `.bbf-locale-switcher__pill--active`) alineado con CVA variants + data-attributes.

**Reemplazar archivo completo:**

```css
/* src/styles/tokens/components/locale-switcher.css */
```

```css
/**
 * BBF Design System — TIER 3 COMPONENT: LocaleSwitcher
 *
 * Subordinado a: B-BBF-WEB-M5-D2-LOCALESWITCHER
 * Decisiones: D-77 (surface-aware), D-78 (state simple), D-82 (AI-readable)
 *
 * Refactor M5-D2: clases canon alineadas con CVA variants.
 *
 * Targets:
 *   .bbf-locale-switcher                  — nav container
 *   .bbf-locale-switcher__pill            — botón locale
 *   .bbf-locale-switcher__pill--active    — variant active state
 *
 * Data attributes:
 *   data-component                        — identificador AI-readable
 *   data-locale                           — código locale del pill
 *   data-active                           — active state
 *   data-surface (en container)           — surface override explícito
 */

/* ============================================================
   CONTAINER
   ============================================================ */
.bbf-locale-switcher {
  --bbf-locale-bg: var(--bbf-surface-glass-light, rgba(255, 255, 255, 0.6));
  --bbf-locale-text: var(--bbf-text-on-light, #1a1a1a);
  --bbf-locale-text-active: var(--bbf-text-on-dark, #ffffff);
  --bbf-locale-bg-active: var(--bbf-surface-black, #1a1a1a);

  display: inline-flex;
  align-items: center;
  position: fixed;
  top: var(--bbf-space-4, 1rem);
  right: var(--bbf-space-4, 1rem);
  z-index: 30;
  padding: var(--bbf-space-1, 0.25rem);
  background: var(--bbf-locale-bg);
  border-radius: var(--bbf-radius-full, 999px);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

/* ============================================================
   PILL (cada botón locale)
   ============================================================ */
.bbf-locale-switcher__pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--bbf-locale-text);
  background: transparent;
  border: none;
  border-radius: var(--bbf-radius-full, 999px);
  cursor: pointer;
  transition:
    background-color 200ms ease,
    color 200ms ease,
    transform 200ms ease;
}

.bbf-locale-switcher__pill:hover:not(:disabled) {
  background: var(--bbf-surface-sand-elevated, rgba(0, 0, 0, 0.05));
}

.bbf-locale-switcher__pill:active:not(:disabled) {
  transform: scale(0.96);
}

/* Active state via class O data-attribute */
.bbf-locale-switcher__pill--active,
.bbf-locale-switcher__pill[data-active] {
  background: var(--bbf-locale-bg-active);
  color: var(--bbf-locale-text-active);
  cursor: default;
}

.bbf-locale-switcher__pill--active:hover,
.bbf-locale-switcher__pill[data-active]:hover {
  background: var(--bbf-locale-bg-active); /* no cambia en hover si active */
}

/* ============================================================
   SURFACE AWARENESS
   data-surface en container O en ancestor
   ============================================================ */
[data-surface="dark"] .bbf-locale-switcher,
.bbf-locale-switcher[data-surface="dark"] {
  --bbf-locale-bg: var(--bbf-surface-glass-dark, rgba(0, 0, 0, 0.4));
  --bbf-locale-text: var(--bbf-text-on-dark, #ffffff);
  --bbf-locale-text-active: var(--bbf-text-on-light, #1a1a1a);
  --bbf-locale-bg-active: var(--bbf-surface-sand, #fdf5ed);
}

[data-surface="glass"] .bbf-locale-switcher,
.bbf-locale-switcher[data-surface="glass"] {
  --bbf-locale-bg: var(--bbf-surface-glass-light, rgba(255, 255, 255, 0.3));
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}

/* ============================================================
   RESPONSIVE — mobile adjust position
   ============================================================ */
@media (max-width: 640px) {
  .bbf-locale-switcher {
    top: var(--bbf-space-3, 0.75rem);
    right: var(--bbf-space-3, 0.75rem);
  }
}

/* ============================================================
   REDUCED MOTION
   ============================================================ */
@media (prefers-reduced-motion: reduce) {
  .bbf-locale-switcher__pill {
    transition: none;
  }
  .bbf-locale-switcher__pill:active:not(:disabled) {
    transform: none;
  }
}
```

---

## §9 — STOP 8: UPDATE page.tsx IMPORT

```bash
# Audit import actual
grep -n "LocaleSwitcher" src/app/\(frontend\)/\[locale\]/page.tsx
```

**Cambio requerido:**

```tsx
// ANTES
import { LocaleSwitcher } from '@/components/i18n/LocaleSwitcher';

// DESPUÉS
import { LocaleSwitcher } from '@/components/molecules/LocaleSwitcher';
```

**JSX uso preservado.** Si page.tsx usa `<LocaleSwitcher />` simple, sigue funcionando idéntico. La API nueva agrega props opcionales.

---

## §10 — STOP 9: ELIMINAR FLAT LEGACY

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

# Audit que no quede ningún import
grep -rn "from '@/components/i18n/LocaleSwitcher'\|from '@/components/i18n'" src/ 2>/dev/null

# Si retorna vacío, eliminar legacy
git rm src/components/i18n/LocaleSwitcher.tsx

# Si dir queda vacío, eliminarlo también
[ -z "$(ls -A src/components/i18n/ 2>/dev/null)" ] && rmdir src/components/i18n/
```

---

## §11 — STOP 10: BUILD + DEV SMOKE TEST

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

pnpm typecheck 2>&1 | tail -3
echo "tsc exit: $?"

echo "N" | pnpm build 2>&1 | tail -15
echo "build exit: $?"
```

### §11.1 Dev smoke test

```bash
pnpm dev > /tmp/bbf-dev.log 2>&1 &
DEV_PID=$!

until curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ 2>/dev/null | grep -q 200; do
  sleep 2
done

echo "═══ HTTP STATUS ═══"
echo "/ ES:   $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/)"
echo "/en EN: $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/en)"

echo ""
echo "═══ LocaleSwitcher HTML renderizado ═══"
curl -s http://localhost:3000/ | grep -oE 'data-component="bbf-locale-switcher"[^>]*' | head -3
curl -s http://localhost:3000/ | grep -oE 'data-component="bbf-locale-switcher-pill"[^>]*' | head -3
curl -s http://localhost:3000/ | grep -oE 'data-locale="[^"]*"' | head -3
curl -s http://localhost:3000/ | grep -oE 'data-active' | head -1

echo ""
echo "═══ Clases CSS canon ═══"
curl -s http://localhost:3000/ | grep -oE 'class="bbf-locale-switcher[^"]*"' | head -3

kill $DEV_PID 2>/dev/null
wait 2>/dev/null
```

### §11.2 Verificaciones críticas

| Test | Esperado |
|------|----------|
| Build exit 0 | ✅ |
| 5/5 páginas estáticas | ✅ |
| / ES → 200 | ✅ |
| /en EN → 200 | ✅ |
| `data-component="bbf-locale-switcher"` HTML presente | ✅ |
| `data-component="bbf-locale-switcher-pill"` HTML presente | ✅ |
| `data-locale="es"` y `data-locale="en"` presentes | ✅ |
| `data-active` en pill activo | ✅ |
| Clases canon `bbf-locale-switcher` y `bbf-locale-switcher__pill` (con espacio entre ellas) | ✅ |
| Sin clases concatenadas legacy bug | ✅ |

### §11.3 Bloqueo

Si build falla o LocaleSwitcher no renderiza correctamente, **REPORTAR Y DETENER**. Puede ser:
- Import path next-intl router incorrecto
- next-intl version conflict con useTransition
- TypeScript type mismatch en Locale

---

## §12 — STOP 11: COMMIT + PUSH

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

git status --short

git add src/components/molecules/
git add src/styles/tokens/components/locale-switcher.css
git add src/app/\(frontend\)/\[locale\]/page.tsx

git status --short

git commit -m "feat(m5-d2): LocaleSwitcher molecule canon — API monolítica

B-BBF-WEB-M5-D2-LOCALESWITCHER

Refactor de LocaleSwitcher flat a molecule canon BBF.
API monolítica simple (D-85 aplica A-01 Simplicidad sobre D-79).

Decisión firmada NUEVA:
- D-BBF-WEB-85: LocaleSwitcher API monolítica (NO compound)
  Razón: A-01 SB_Law Simplicidad Primero. Compound se reserva
  para molecules complejas (HeroVideo M5-D3 Source + Overlay).

Decisiones aplicadas:
- D-BBF-WEB-77: surface-aware (data-surface ancestor + variant override)
- D-BBF-WEB-78: state simple (router state via next-intl)
- D-BBF-WEB-82: AI-readable (JSDoc + data-component + data-locale)
- D-BBF-WEB-83: Atomic Design folder structure

Cambios:

Estructura canon:
- src/components/molecules/LocaleSwitcher/
  ├── LocaleSwitcher.tsx
  ├── LocaleSwitcher.variants.ts
  └── index.ts
- src/components/molecules/index.ts (barrel)

Atom LocaleSwitcher refactor:
- API monolítica: <LocaleSwitcher locales={['es','en']} />
- 'use client' (router state)
- useTransition para routing M4 preservado
- next-intl router.replace pathname
- data-component, data-locale, data-active attributes
- aria-label bilingüe canon
- JSDoc con 4 @example

CSS canon:
- locale-switcher.css refactor BEM legacy → canon BBF
- Clases: .bbf-locale-switcher + .bbf-locale-switcher__pill
- Surface awareness vía [data-surface] ancestor o attr propio
- Active state vía class O data-active attribute
- Responsive mobile + reduced-motion

Eliminados:
- src/components/i18n/LocaleSwitcher.tsx (migrado)
- src/components/i18n/ (dir vacío)

Page.tsx:
- Import path: @/components/i18n/LocaleSwitcher → @/components/molecules/LocaleSwitcher
- JSX preservado (API backward-compatible)

TDs resueltas:
- TD-M5-D2-01: clases CSS concatenadas sin espacio (legacy bug)

Verificación:
- tsc --noEmit: exit 0
- pnpm build: exit 0 (5/5 páginas)
- ES↔EN routing M4 preservado funcional
- HTML: data-component, data-locale, data-active presentes
- Sin clases concatenadas legacy

Refs: B-BBF-WEB-M5-D2-LOCALESWITCHER
Refs: BBF_M5_D_Plan.md §3 (M5-D2)"

git push origin feat/m5-design-system

git log --oneline -5
```

---

## §13 — REPORTE FINAL ESPERADO

```
═══ M5-D2 COMPLETADO ═══

Branch: feat/m5-design-system
Commit: <hash>
Push: ✓ origin

LocaleSwitcher molecule canon:
  ✓ src/components/molecules/LocaleSwitcher/
  ✓ API monolítica D-85
  ✓ Surface awareness híbrido (data-attribute + variant)
  ✓ M4 routing ES↔EN preservado 100%
  ✓ data-component + data-locale + data-active attributes

CSS canon:
  ✓ locale-switcher.css refactor (BEM legacy → canon BBF)
  ✓ Sin clases concatenadas (TD-M5-D2-01 resuelta)
  ✓ Surface awareness operativo

Eliminados:
  ✓ src/components/i18n/LocaleSwitcher.tsx
  ✓ src/components/i18n/ dir

Verificación:
  ✓ tsc + build exit 0
  ✓ 5/5 páginas estáticas
  ✓ ES↔EN funcional
  ✓ HTML attributes presentes

LISTO STRATEGIC VERIFICACIÓN VISUAL VERCEL.

Checklist visual:
  - LocaleSwitcher aparece top-right ✓
  - ES y EN pills visibles ✓
  - Pill activo destacado (negro + texto blanco) ✓
  - Click ES→EN funciona y viceversa ✓
  - Cambio idioma actualiza headline/CTA texto ✓
  - Glass effect preservado ✓
  - 0 console errors ✓

Si OK → autorizar M5-D3 (HeroVideo molecule compound).
```

---

## §14 — ESCALATION RULES

**Detener y reportar si:**
- next-intl router hook falla con error de tipos
- Routing ES↔EN se rompe (pierde state o redirecciona mal)
- Layout shift visible (posición pierde su lugar)
- Glass effect desaparece
- Build falla con error de import path

**Auto-corrección autorizada:**
- Ajuste menor del CSS (gap, padding, border-radius)
- Naming convention de archivos
- Naming variants si conflicto con shadcn
- Import absoluto vs relativo

---

## §15 — METADATA

- **Despacho:** B-BBF-WEB-M5-D2-LOCALESWITCHER
- **Versión:** v1.0
- **Fecha:** 2026-05-17
- **HEAD pre:** `e97f2e3`
- **STOPs:** 11
- **Decisiones aplicadas:** D-77, D-78, D-82, D-83, **D-85 NUEVA**
- **TDs resueltas:** TD-M5-D2-01 (clases concatenadas)
- **TDs heredadas:** TD-M5-D1.5-02, TD-M5-D1.5-05 (no scope)
- **Riesgo:** Medio (preservar M4 routing)
- **Próximo:** Strategic audit → M5-D3 HeroVideo molecule

---

*B-BBF-WEB-M5-D2 — LocaleSwitcher molecule canon*
*API monolítica + Surface-aware + M4 preservado*