# COPY_PASTE_B-BBF-WEB-M5-E-MOTION-SYSTEM.md

**Despacho:** B-BBF-WEB-M5-E-MOTION-SYSTEM
**Tipo:** Motion system foundation + WAAPI implementations
**Para:** Claude Code en VS Code
**Repo:** `/Volumes/PK/BBF/Repos/bbf-web/`
**Branch:** `feat/m5-design-system`
**HEAD pre:** `bf5ff6f` (M5-D6 cerrado)
**Tiempo estimado:** 4-6h
**STOPs:** 14
**Riesgo:** Medio (Client Component + JS interactivo + visual changes)

---

## §0 — INSTRUCCIONES OPERATIVAS

### §0.1 Acciones autónomas previas

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

mkdir -p .claude/feedback-archive
[ -f feedback.md ] && mv feedback.md .claude/feedback-archive/feedback_$(date +%Y-%m-%d_%H%M)_pre-M5-E.md

git branch --show-current
git log --oneline -3
```

### §0.2 Disciplina canon

- STOPs respetados (14 secuenciales)
- Auto-corrección §14 autorizada
- **Visual changes esperados:** logo hover smooth (sin "salto") + hero-entrance pulido
- Si emerge regresión visual NO esperada, REPORTAR antes de commit

### §0.3 Decisiones canon que rigen M5-E

| ID | Aplicación |
|----|------------|
| D-77 | Surface-awareness (motion respeta surface context) |
| D-82 | AI-readable (data-component motion attributes) |
| D-91 | Hero composition canon |
| **D-98 NUEVA** | Motion system canon BBF: tokens + WAAPI hybrid |
| **D-99 NUEVA** | BBFLogo split Server (load SVG) + Client (animation control) |

### §0.4 Investigación canon (MODO 3)

Strategic R-BBF-09 ratifica:
- WAAPI `updatePlaybackRate()` es **canon 2026** para smooth playback speed changes
- Animations <300ms canon (excepto rotation infinita)
- transform + opacity only (no layout properties)
- prefers-reduced-motion crítico WCAG 2.2
- will-change sparingly (memory cost)

---

## §1 — OBJETIVO

1. Sistema canon de motion tokens (durations + easings + delays)
2. WAAPI BBFLogo split (Server SVG load + Client animation)
3. Logo hover smooth (TD-M5-D1.5-05 — el "salto" detectado por Zavala)
4. Hero-entrance migration a motion system canon (TD-M5-D4-01)
5. Animation patterns canon BBF (entry, hover, focus, scroll)
6. Visual: logo hover smooth ✓, hero-entrance pulido ✓, otros preservados

---

## §2 — STOP 1: PRE-EXECUTION AUDIT

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "═══ Branch + HEAD ═══"
git branch --show-current
git log --oneline -3
# Esperado: bf5ff6f

echo ""
echo "═══ tsc clean ═══"
pnpm typecheck 2>&1 | tail -3

echo ""
echo "═══ 1. Motion tokens existentes ═══"
echo "--- semantic motion.css ---"
cat src/styles/tokens/semantic/motion.css 2>/dev/null || echo "NO EXISTE"
echo ""
echo "--- hero.css motion vars ---"
grep -B1 -A1 "bbf-motion\|bbf.*duration\|bbf.*easing" src/styles/tokens/components/hero.css | head -30

echo ""
echo "═══ 2. BBFLogo atom estructura ═══"
ls -la src/components/atoms/BBFLogo/
echo ""
cat src/components/atoms/BBFLogo/BBFLogo.tsx | head -50

echo ""
echo "═══ 3. logo.css animation rule ═══"
grep -B1 -A 10 "@keyframes bbf-logo-rotate\|animation-duration\|bbf-logo-rotation-speed" src/styles/tokens/components/logo.css

echo ""
echo "═══ 4. hero-entrance CSS animations (TD-M5-D4-01) ═══"
grep -B1 -A 5 "hero-entrance\|@keyframes hero-entrance" src/styles/utilities/animations.css 2>/dev/null
echo ""
grep -B1 -A 5 "hero-entrance" src/styles/tokens/components/hero.css | head -30

echo ""
echo "═══ 5. animations.css completo ═══"
cat src/styles/utilities/animations.css 2>/dev/null || echo "NO EXISTE"

echo ""
echo "═══ 6. Tokens easing canon (verificar primitives) ═══"
grep -rn "cubic-bezier\|bbf.*easing\|bbf.*cubic" src/styles/ 2>/dev/null | head -10
```

### §2.1 Reporte STOP 1 esperado

```
[STOP 1 completado]

Branch: feat/m5-design-system ✓
HEAD: bf5ff6f ✓
tsc: exit 0 ✓

Hallazgos:
  Motion tokens existentes: [lista de --bbf-motion-* y locations]
  BBFLogo atom: Server Component (enrichSvg + readFileSync probable)
  logo.css animation: @keyframes bbf-logo-rotate + animation-duration variable
  hero-entrance: animaciones CSS en [archivo, líneas]
  animations.css: [existe/no, contenido]
  Easing tokens: [valores actuales]

LISTO para STOP 2.
```

### §2.2 Bloqueo

Si BBFLogo NO es Server Component (ya es Client), reportar — Strategic ajusta D-99.

---

## §3 — STOP 2: CREAR semantic/motion.css

```bash
ls -la src/styles/tokens/semantic/motion.css 2>/dev/null
```

Si ya existe (M5-D5 lo mencionaba en imports), audit + extender. Si NO existe, crear.

**Contenido canon completo:**

```css
/* src/styles/tokens/semantic/motion.css */
```

```css
/**
 * BBF Design System — SEMANTIC TOKEN: Motion
 *
 * Subordinado a: B-BBF-WEB-M5-E-MOTION-SYSTEM
 * Decisiones: D-98 NUEVA (motion system canon BBF)
 *
 * Sistema canon de motion tokens BBF:
 *   - Durations: 5 niveles (instant, fast, base, slow, slower)
 *   - Easings: 4 curves canon (linear, in, out, inOut)
 *   - Custom curves: BBF signature (entrance, exit, hover)
 *   - Delays: stagger pattern (75ms cascade canon)
 *
 * Pattern canon Tailwind v4:
 *   - CSS: transition: transform var(--bbf-motion-fast) var(--bbf-easing-out);
 *   - JSX: [transition:transform_var(--bbf-motion-fast)_var(--bbf-easing-out)]
 *
 * Reglas canon BBF (R-BBF-09 motion best practices 2026):
 *   - Target duration: <300ms para hover/feedback (perceived performance)
 *   - Continuous animations (rotation): pueden ser >300ms (no afecta perceived)
 *   - Animate ONLY: transform + opacity (no layout properties)
 *   - prefers-reduced-motion siempre respetar (WCAG 2.2)
 *   - will-change: sparingly + remove after animation
 */

:root {
  /* ============================================================
     DURATIONS canon (5 niveles)
     ============================================================ */
  --bbf-motion-instant: 0ms;       /* state changes sin transition */
  --bbf-motion-fast: 150ms;        /* hover, focus, small UI feedback */
  --bbf-motion-base: 250ms;        /* default canon BBF (95% casos) */
  --bbf-motion-slow: 400ms;        /* entrance animations, modals */
  --bbf-motion-slower: 600ms;      /* full transitions, page leaves */

  /* ============================================================
     EASINGS canon (4 curves estándar + custom BBF)
     ============================================================ */
  --bbf-easing-linear: linear;
  --bbf-easing-in: cubic-bezier(0.4, 0, 1, 1);
  --bbf-easing-out: cubic-bezier(0, 0, 0.2, 1);
  --bbf-easing-in-out: cubic-bezier(0.4, 0, 0.2, 1);

  /* BBF signature curves (premium feel) */
  --bbf-easing-entrance: cubic-bezier(0.16, 1, 0.3, 1);  /* smooth ease-out premium */
  --bbf-easing-exit: cubic-bezier(0.7, 0, 0.84, 0);      /* ease-in quick */
  --bbf-easing-hover: cubic-bezier(0.4, 0, 0.2, 1);      /* feedback responsivo */
  --bbf-easing-bounce: cubic-bezier(0.34, 1.56, 0.64, 1); /* sutil spring */

  /* ============================================================
     DELAYS canon (stagger pattern)
     ============================================================ */
  --bbf-stagger-base: 75ms;        /* stagger entre hijos cascade */
  --bbf-delay-1: calc(var(--bbf-stagger-base) * 1);  /* 75ms */
  --bbf-delay-2: calc(var(--bbf-stagger-base) * 2);  /* 150ms */
  --bbf-delay-3: calc(var(--bbf-stagger-base) * 3);  /* 225ms */
  --bbf-delay-4: calc(var(--bbf-stagger-base) * 4);  /* 300ms */
  --bbf-delay-5: calc(var(--bbf-stagger-base) * 5);  /* 375ms */

  /* ============================================================
     SEMANTIC ALIASES (uso contextual canon BBF)
     ============================================================ */
  --bbf-transition-default: all var(--bbf-motion-base) var(--bbf-easing-in-out);
  --bbf-transition-hover: transform var(--bbf-motion-fast) var(--bbf-easing-hover);
  --bbf-transition-color: color var(--bbf-motion-fast) var(--bbf-easing-out),
                          background-color var(--bbf-motion-fast) var(--bbf-easing-out);
  --bbf-transition-fade: opacity var(--bbf-motion-base) var(--bbf-easing-out);
  --bbf-transition-entrance: opacity var(--bbf-motion-slow) var(--bbf-easing-entrance),
                              transform var(--bbf-motion-slow) var(--bbf-easing-entrance);

  /* ============================================================
     LOGO ROTATION canon (continuous animation, override visible)
     ============================================================ */
  --bbf-logo-rotation-duration: 40s;          /* idle rotation speed */
  --bbf-logo-rotation-duration-hover: 12s;    /* hover acceleration target */
  --bbf-logo-rotation-easing: var(--bbf-easing-linear); /* rotation NUNCA ease */
}

/* ============================================================
   REDUCED MOTION (WCAG 2.2 canon)
   ============================================================ */
@media (prefers-reduced-motion: reduce) {
  :root {
    --bbf-motion-fast: 0.01ms;
    --bbf-motion-base: 0.01ms;
    --bbf-motion-slow: 0.01ms;
    --bbf-motion-slower: 0.01ms;
    --bbf-logo-rotation-duration: 0s;
    --bbf-logo-rotation-duration-hover: 0s;
  }
}
```

---

## §4 — STOP 3: IMPORT motion.css EN GLOBALS

```bash
grep -n "motion.css\|@import" src/app/globals.css | head -10
```

Si motion.css NO está importado, agregar en orden canon (semantic tier, después de typography y spacing).

Si ya está importado pero apunta a archivo vacío/inexistente, ahora se llena con contenido canon.

---

## §5 — STOP 4: UPDATE logo.css USAR TOKENS CANON

```bash
cat src/styles/tokens/components/logo.css
```

**Strategic estrategia:** Migrar valores hardcoded (40s, 12s, linear) a tokens canon de motion.css. Pattern canon.

**Reemplazos esperados en logo.css:**

```css
/* BUSCAR */
.bbf-logo--stamp[data-animated="true"] .bbf-logo-name-circle {
  animation: bbf-logo-rotate var(--bbf-logo-rotation-speed, 40s) linear infinite;
  ...
}

/* REEMPLAZAR */
.bbf-logo--stamp[data-animated="true"] .bbf-logo-name-circle {
  animation: bbf-logo-rotate var(--bbf-logo-rotation-duration, 40s)
             var(--bbf-easing-linear, linear) infinite;
  ...
}
```

```css
/* BUSCAR */
@media (hover: hover) {
  .bbf-logo--stamp[data-animated="true"]:hover .bbf-logo-name-circle,
  a:hover .bbf-logo--stamp[data-animated="true"] .bbf-logo-name-circle {
    animation-duration: var(--bbf-logo-rotation-speed-hover, 12s);
  }
  ...
}
```

**Eliminar bloque @media hover completo** porque WAAPI lo manejará desde Client Component (más smooth).

**Reemplazar por nada** — WAAPI controlará la velocidad.

**Hover scale preservar** (es transform, no rotation):

```css
@media (hover: hover) {
  .bbf-logo--stamp[data-animated="true"]:hover,
  a:hover .bbf-logo--stamp[data-animated="true"] {
    transform: scale(var(--bbf-logo-hover-scale, 1.04));
    transition: var(--bbf-transition-hover);
  }
}
```

---

## §6 — STOP 5: SPLIT BBFLogo SERVER + CLIENT (D-99 NUEVA)

**Strategic decisión doctrinal D-99 NUEVA:**

BBFLogo actual es Server Component (lee SVG con `fs.readFileSync`). Para WAAPI necesitamos Client Component. **Pattern canon BBF:**

- `BBFLogo.tsx` — Server Component (load SVG inline) ← preservar
- `BBFLogoAnimator.tsx` — Client Component wrapper (WAAPI control) ← NUEVO

### §6.1 Crear `BBFLogoAnimator.tsx`

```typescript
// src/components/atoms/BBFLogo/BBFLogoAnimator.tsx
```

**Contenido exacto:**

```typescript
/**
 * BBF Design System — BBFLogoAnimator (Client Component)
 *
 * Subordinado a: B-BBF-WEB-M5-E-MOTION-SYSTEM
 * Decisiones: D-99 NUEVA (BBFLogo split Server + Client)
 *
 * Wrapper Client Component que aplica WAAPI a BBFLogo Server.
 * Resuelve TD-M5-D1.5-05: hover speed transition smooth via updatePlaybackRate.
 *
 * Pattern canon BBF:
 *   - Server Component carga SVG (preserva static rendering, RSC compatible)
 *   - Client Component wrappea + aplica WAAPI animation control
 *   - Composition canon: import { BBFLogo } y wrap si necesita animation control
 *
 * NOTA: Solo necesario cuando se requiere hover speed control smooth.
 * Para logo estático (no animated), usar BBFLogo directo.
 *
 * @example
 * ```tsx
 * import { BBFLogo } from './BBFLogo';
 * import { BBFLogoAnimator } from './BBFLogoAnimator';
 *
 * <BBFLogoAnimator>
 *   <BBFLogo variant="stamp" size="hero" animated />
 * </BBFLogoAnimator>
 * ```
 */

'use client';

import { useEffect, useRef, type ReactNode } from 'react';

// Constantes canon BBF (alineadas con motion.css tokens)
const ROTATION_DURATION_IDLE = 40000;   // 40s — token canon
const ROTATION_DURATION_HOVER = 12000;  // 12s — token canon
const PLAYBACK_RATE_IDLE = 1;
const PLAYBACK_RATE_HOVER = ROTATION_DURATION_IDLE / ROTATION_DURATION_HOVER; // 3.33x

export interface BBFLogoAnimatorProps {
  /**
   * Children: BBFLogo Server Component (variant=stamp + animated=true esperado).
   */
  children: ReactNode;
}

/**
 * BBFLogoAnimator — Client wrapper para WAAPI canon
 *
 * Toma control de la animación CSS infinite rotate del SVG name-circle
 * y aplica WAAPI updatePlaybackRate() en hover para smooth speed change.
 *
 * Sin "salto" perceptible al cambiar velocidad (resuelve TD-M5-D1.5-05).
 */
export function BBFLogoAnimator({ children }: BBFLogoAnimatorProps) {
  const wrapperRef = useRef<HTMLDivElement>(null);
  const animationRef = useRef<Animation | null>(null);

  useEffect(() => {
    if (!wrapperRef.current) return;

    // Verificar prefers-reduced-motion
    const mediaQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
    if (mediaQuery.matches) return;

    // Buscar el SVG name-circle dentro de los children
    const nameCircle = wrapperRef.current.querySelector('.bbf-logo-name-circle') as SVGElement | null;
    if (!nameCircle) return;

    // Tomar control: cancelar CSS animation, iniciar WAAPI
    nameCircle.style.animation = 'none';

    const animation = nameCircle.animate(
      [
        { transform: 'rotate(0deg)' },
        { transform: 'rotate(360deg)' },
      ],
      {
        duration: ROTATION_DURATION_IDLE,
        iterations: Infinity,
        easing: 'linear',
      }
    );

    animationRef.current = animation;

    return () => {
      animation.cancel();
      animationRef.current = null;
      // Restaurar CSS animation si componente unmount
      if (nameCircle) {
        nameCircle.style.animation = '';
      }
    };
  }, []);

  // Hover handlers via React events (smooth playback rate change)
  const handleMouseEnter = () => {
    if (animationRef.current) {
      animationRef.current.updatePlaybackRate(PLAYBACK_RATE_HOVER);
    }
  };

  const handleMouseLeave = () => {
    if (animationRef.current) {
      animationRef.current.updatePlaybackRate(PLAYBACK_RATE_IDLE);
    }
  };

  return (
    <div
      ref={wrapperRef}
      data-component="bbf-logo-animator"
      onMouseEnter={handleMouseEnter}
      onMouseLeave={handleMouseLeave}
      style={{ display: 'inline-flex' }}
    >
      {children}
    </div>
  );
}
```

### §6.2 Update barrel `index.ts`

```typescript
// src/components/atoms/BBFLogo/index.ts
```

Agregar export:

```typescript
export { BBFLogo } from './BBFLogo';
export { BBFLogoAnimator } from './BBFLogoAnimator';
export type { BBFLogoProps } from './BBFLogo';
export type { BBFLogoAnimatorProps } from './BBFLogoAnimator';
// ... otros exports existentes
```

---

## §7 — STOP 6: UPDATE page.tsx WRAP BBFLogo

```bash
grep -n "BBFLogo" src/app/\(frontend\)/\[locale\]/page.tsx
```

**Cambio:**

```tsx
// ANTES
import { BBFLogo } from '@/components/atoms/BBFLogo';
// ...
<div className="hero-entrance hero-entrance--delay-1">
  <BBFLogo variant="stamp" size="hero" animated />
</div>

// DESPUÉS
import { BBFLogo, BBFLogoAnimator } from '@/components/atoms/BBFLogo';
// ...
<div className="hero-entrance hero-entrance--delay-1">
  <BBFLogoAnimator>
    <BBFLogo variant="stamp" size="hero" animated />
  </BBFLogoAnimator>
</div>
```

---

## §8 — STOP 7: MIGRAR hero-entrance A MOTION CANON (TD-M5-D4-01)

```bash
grep -B2 -A 10 "hero-entrance" src/styles/utilities/animations.css 2>/dev/null
grep -B2 -A 10 "hero-entrance" src/styles/tokens/components/hero.css 2>/dev/null
```

**Strategic estrategia:**

Las animaciones `hero-entrance` legacy usan probablemente valores hardcoded. Migrar a tokens canon motion.

**Patch esperado en animations.css o hero.css:**

```css
/* BUSCAR pattern legacy */
@keyframes hero-entrance {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-entrance {
  opacity: 0;
  animation: hero-entrance 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
}

.hero-entrance--delay-1 { animation-delay: 0.1s; }
.hero-entrance--delay-2 { animation-delay: 0.2s; }
.hero-entrance--delay-3 { animation-delay: 0.3s; }
.hero-entrance--delay-4 { animation-delay: 0.4s; }
```

**Reemplazar con canon BBF:**

```css
/* ============================================================
   HERO ENTRANCE canon BBF (M5-E migration)
   Subordinado a: B-BBF-WEB-M5-E (D-98 motion canon)
   TD-M5-D4-01 resuelta
   ============================================================ */
@keyframes hero-entrance {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-entrance {
  opacity: 0;
  animation: hero-entrance var(--bbf-motion-slow, 400ms)
             var(--bbf-easing-entrance, cubic-bezier(0.16, 1, 0.3, 1))
             forwards;
  will-change: opacity, transform;
}

/* Stagger delays canon (75ms base) */
.hero-entrance--delay-1 { animation-delay: var(--bbf-delay-1, 75ms); }
.hero-entrance--delay-2 { animation-delay: var(--bbf-delay-2, 150ms); }
.hero-entrance--delay-3 { animation-delay: var(--bbf-delay-3, 225ms); }
.hero-entrance--delay-4 { animation-delay: var(--bbf-delay-4, 300ms); }
.hero-entrance--delay-5 { animation-delay: var(--bbf-delay-5, 375ms); }

/* Remove will-change post-animation (canon performance) */
.hero-entrance.is-complete {
  will-change: auto;
}

/* WCAG 2.2 reduced motion */
@media (prefers-reduced-motion: reduce) {
  .hero-entrance {
    opacity: 1;
    transform: none;
    animation: none;
  }
}
```

**Auto-corrección §14 autorizada:** Si el legacy tiene values específicos diferentes (delays mayores, etc.), preservar comportamiento adaptando tokens canon.

---

## §9 — STOP 8: CREAR PATTERNS canon (Strategic doctrinal)

Strategic establece patterns canon BBF que future-proof el motion system. **Estos NO se usan ahora**, pero quedan documentados como canon para M6+.

### §9.1 Crear `src/styles/utilities/motion-patterns.css`

```css
/**
 * BBF Design System — Motion patterns canon
 *
 * Subordinado a: B-BBF-WEB-M5-E-MOTION-SYSTEM
 * Decisión: D-98 NUEVA (motion canon BBF)
 *
 * Patterns reusables canon para futuros components (M6+).
 * Aplicar con className en JSX o componentes.
 *
 * Reglas canon (R-BBF-09 ratificada):
 *   - Animate solo transform + opacity
 *   - <300ms para hover/feedback
 *   - prefers-reduced-motion siempre
 *   - will-change solo en animation activa
 */

/* ============================================================
   PATTERN: fade-in entrance
   ============================================================ */
.bbf-fade-in {
  opacity: 0;
  animation: bbf-fade-in var(--bbf-motion-slow) var(--bbf-easing-out) forwards;
}

@keyframes bbf-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* ============================================================
   PATTERN: slide-up entrance
   ============================================================ */
.bbf-slide-up {
  opacity: 0;
  transform: translateY(20px);
  animation: bbf-slide-up var(--bbf-motion-slow) var(--bbf-easing-entrance) forwards;
}

@keyframes bbf-slide-up {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ============================================================
   PATTERN: scale-in entrance (modal, popover)
   ============================================================ */
.bbf-scale-in {
  opacity: 0;
  transform: scale(0.95);
  animation: bbf-scale-in var(--bbf-motion-base) var(--bbf-easing-entrance) forwards;
}

@keyframes bbf-scale-in {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* ============================================================
   PATTERN: hover-lift (cards, interactive elements)
   ============================================================ */
.bbf-hover-lift {
  transition: transform var(--bbf-motion-fast) var(--bbf-easing-hover),
              box-shadow var(--bbf-motion-fast) var(--bbf-easing-hover);
}

@media (hover: hover) {
  .bbf-hover-lift:hover {
    transform: translateY(-2px);
    box-shadow: var(--bbf-shadow-md);
  }
}

/* ============================================================
   PATTERN: focus-ring canon BBF
   ============================================================ */
.bbf-focus-ring {
  transition: outline var(--bbf-motion-fast) var(--bbf-easing-out);
}

.bbf-focus-ring:focus-visible {
  outline: 2px solid var(--bbf-color-focus-ring, oklch(0.6 0.22 257.43));
  outline-offset: 2px;
}

/* ============================================================
   REDUCED MOTION canon WCAG 2.2
   ============================================================ */
@media (prefers-reduced-motion: reduce) {
  .bbf-fade-in,
  .bbf-slide-up,
  .bbf-scale-in {
    opacity: 1;
    transform: none;
    animation: none;
  }

  .bbf-hover-lift:hover {
    transform: none;
  }
}
```

### §9.2 Import en globals.css

```css
/* En globals.css agregar */
@import '../styles/utilities/motion-patterns.css';
```

---

## §10 — STOP 9: VERIFICACIÓN POST-EDIT

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "═══ 1. motion.css existe con tokens canon ═══"
grep -c "bbf-motion\|bbf-easing\|bbf-delay" src/styles/tokens/semantic/motion.css

echo ""
echo "═══ 2. BBFLogoAnimator creado ═══"
ls -la src/components/atoms/BBFLogo/BBFLogoAnimator.tsx

echo ""
echo "═══ 3. page.tsx usa BBFLogoAnimator wrap ═══"
grep -B1 -A 4 "BBFLogoAnimator" src/app/\(frontend\)/\[locale\]/page.tsx

echo ""
echo "═══ 4. hero-entrance migration ═══"
grep -B1 -A 3 "hero-entrance" src/styles/utilities/animations.css 2>/dev/null | head -20

echo ""
echo "═══ 5. motion-patterns.css existe ═══"
ls -la src/styles/utilities/motion-patterns.css

echo ""
echo "═══ 6. Imports en globals.css ═══"
grep -n "@import" src/app/globals.css | grep -i "motion\|animations"
```

---

## §11 — STOP 10: BUILD + DEV SMOKE

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

pnpm typecheck 2>&1 | tail -3
echo "tsc exit: $?"

echo "N" | pnpm build 2>&1 | tail -12
echo "build exit: $?"

# Dev smoke
pnpm dev > /tmp/bbf-dev.log 2>&1 &
DEV_PID=$!

until curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ 2>/dev/null | grep -q 200; do
  sleep 2
done

echo ""
echo "═══ HTTP STATUS ═══"
echo "/ ES:   $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/)"
echo "/en EN: $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/en)"

echo ""
echo "═══ BBFLogoAnimator wrapper en HTML ═══"
curl -s http://localhost:3000/ | grep -oE 'data-component="bbf-logo-animator"[^>]*' | head -1

echo ""
echo "═══ BBFLogo dentro del Animator ═══"
curl -s http://localhost:3000/ | grep -oE 'data-component="bbf-logo"[^>]*' | head -1

echo ""
echo "═══ CSS bundle: motion tokens ═══"
grep -c "bbf-motion-base\|bbf-easing-entrance" .next/static/css/*.css 2>/dev/null

kill $DEV_PID 2>/dev/null
wait 2>/dev/null
```

---

## §12 — STOP 11: VISUAL VERIFICATION (manual hint)

**Strategic indica a Zavala que verificará en Vercel preview:**

- Logo idle: rotación normal velocidad
- Logo hover: aceleración SMOOTH (sin "salto" inicial)
- Logo unhover: desaceleración SMOOTH (sin "salto" final)
- Hero entrance: animaciones suaves entrada (delay cascade 75ms)
- prefers-reduced-motion (devtools): animaciones detenidas
- Console: 0 errores

---

## §13 — STOP 12: COMMIT + PUSH

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

git status --short

git add src/styles/tokens/semantic/motion.css
git add src/styles/utilities/animations.css 2>/dev/null
git add src/styles/utilities/motion-patterns.css
git add src/styles/tokens/components/logo.css
git add src/components/atoms/BBFLogo/BBFLogoAnimator.tsx
git add src/components/atoms/BBFLogo/index.ts
git add src/app/\(frontend\)/\[locale\]/page.tsx
git add src/app/globals.css

git status --short

git commit -m "feat(m5-e): motion system canon BBF + WAAPI logo hover smooth

B-BBF-WEB-M5-E-MOTION-SYSTEM

Sistema canon de motion + WAAPI implementation para logo hover.
Resuelve TDs latentes desde M5-D1.5 y M5-D4.

Decisiones firmadas NUEVAS:
- D-BBF-WEB-98: Motion system canon BBF
  Sistema de tokens canon: 5 durations + 4 easings standard +
  4 BBF signatures + 5 delays stagger + 5 aliases semánticos.
  Pattern Tailwind v4: [property:var(--token)].
  Reglas: <300ms hover, transform+opacity only, reduced-motion.

- D-BBF-WEB-99: BBFLogo split Server + Client canon
  BBFLogo Server Component (load SVG) + BBFLogoAnimator Client
  (WAAPI animation control). Composition pattern canon para
  components que requieren JS interactivo sobre RSC.

TDs resueltas:
- TD-M5-D1.5-05: Hover speed transition WAAPI smooth
  Antes: CSS animation-duration change → 'salto' visible
  Después: WAAPI updatePlaybackRate() → smooth transition
  ratificada por R-BBF-09 (MDN canon 2026).

- TD-M5-D4-01: hero-entrance CSS migration a motion canon
  Antes: valores hardcoded 0.6s cubic-bezier()
  Después: tokens canon var(--bbf-motion-slow) var(--bbf-easing-entrance)

Cambios:

Sistema canon nuevo:
- src/styles/tokens/semantic/motion.css
  ├── Durations: instant/fast/base/slow/slower
  ├── Easings: linear/in/out/inOut + BBF signatures
  ├── Delays: 5 levels stagger 75ms base
  ├── Semantic aliases: 5 contextual (default/hover/color/fade/entrance)
  └── Logo rotation tokens dedicated

- src/styles/utilities/motion-patterns.css NUEVO
  ├── Patterns canon BBF: fade-in/slide-up/scale-in/hover-lift/focus-ring
  ├── Reusables para M6+ components
  └── WCAG 2.2 prefers-reduced-motion canon

- src/components/atoms/BBFLogo/BBFLogoAnimator.tsx NUEVO (Client)
  ├── WAAPI control de rotación logo stamp
  ├── updatePlaybackRate canon 2026 (sin 'salto')
  ├── prefers-reduced-motion respect
  ├── 12s hover / 40s idle (playback rate 3.33x)
  └── Cleanup en unmount canon

Migraciones:
- logo.css: hardcoded values → tokens motion canon
- animations.css o hero.css: hero-entrance → tokens motion canon
- page.tsx: BBFLogo wrappeado con BBFLogoAnimator

Research canon:
- R-BBF-09 NUEVA: WAAPI best practices 2026 (MDN, motion guidelines)
- updatePlaybackRate canon para smooth speed transitions
- Animate transform+opacity only (no layout)
- <300ms hover/feedback target
- prefers-reduced-motion crítico WCAG 2.2

Lecciones acumuladas:
- L-BBF-98: Foundations se construyen cuando ≥3 casos justifican
  (motion system: logo hover + hero-entrance + future patterns = 3+ casos)

Verificación:
- tsc + build exit 0 (5/5 páginas)
- HTTP 200 ES + EN
- BBFLogoAnimator wrapper presente en HTML
- Motion tokens compilados en bundle
- Visual: logo hover smooth ✓, hero-entrance pulido ✓

Refs: B-BBF-WEB-M5-E-MOTION-SYSTEM
Refs: D-98, D-99 NUEVAS
Refs: TD-M5-D1.5-05, TD-M5-D4-01 RESUELTAS
Refs: R-BBF-09 motion best practices 2026"

git push origin feat/m5-design-system
git log --oneline -3
```

---

## §14 — STOP 13: REPORTE FINAL

```
═══ M5-E COMPLETADO ═══

Branch: feat/m5-design-system
Commit: <hash>
Push: ✓ origin

Sistema canon motion (D-98):
  ✓ semantic/motion.css (5 durations + 4 easings + 4 BBF signatures + 5 delays + 5 aliases)
  ✓ utilities/motion-patterns.css (5 patterns canon BBF)
  ✓ Pattern Tailwind v4 documentado
  ✓ R-BBF-09 best practices 2026 aplicadas

WAAPI BBFLogo (D-99):
  ✓ BBFLogo Server Component preservado
  ✓ BBFLogoAnimator Client Component NUEVO
  ✓ updatePlaybackRate canon (sin 'salto')
  ✓ prefers-reduced-motion respect
  ✓ Cleanup canon

Migraciones:
  ✓ logo.css: hardcoded → tokens canon
  ✓ hero-entrance: tokens canon BBF
  ✓ page.tsx: BBFLogoAnimator wrap

TDs resueltas:
  ✓ TD-M5-D1.5-05 (WAAPI logo hover smooth)
  ✓ TD-M5-D4-01 (hero-entrance migration)

Verificación:
  ✓ tsc + build exit 0 (5/5 páginas)
  ✓ HTTP 200 ES + EN
  ✓ BBFLogoAnimator presente en HTML
  ✓ Motion tokens en bundle

LISTO STRATEGIC VERIFICACIÓN VISUAL VERCEL.

Checklist visual (cambios esperados):
  - Logo idle: rotación normal preserved ✓
  - Logo hover SMOOTH (sin salto): aceleración 3.33x via WAAPI ✓
  - Logo unhover SMOOTH: desaceleración via updatePlaybackRate ✓
  - Hero entrance: animaciones canon BBF preserved ✓
  - prefers-reduced-motion: animations off (verificable devtools) ✓
  - 0 console errors ✓

Si OK → autorizar M5-F (AI-readable docs comprehensivo).
```

---

## §15 — ESCALATION RULES

**Detener y reportar si:**
- BBFLogo NO es Server Component (ya es Client) → ajustar D-99
- WAAPI no soportado en browser target (raro pero verificar)
- updatePlaybackRate produce comportamiento inesperado
- prefers-reduced-motion no respeta
- Hero-entrance animations rotas post-migration
- Build falla
- Visual cambia perceptiblemente fuera de scope esperado

**Auto-corrección §14 autorizada:**
- Path real archivo hero-entrance (animations.css vs hero.css)
- Naming canon archivos
- Valores específicos legacy a preservar
- Si BBFLogo tiene API diferente, adaptar BBFLogoAnimator
- React DOM events spelling

---

## §16 — METADATA

- **Despacho:** B-BBF-WEB-M5-E-MOTION-SYSTEM
- **Versión:** v1.0
- **Fecha:** 2026-05-17
- **HEAD pre:** `bf5ff6f`
- **STOPs:** 14
- **Decisiones aplicadas:** D-77, D-82, D-91, **D-98 NUEVA**, **D-99 NUEVA**
- **TDs resueltas:** TD-M5-D1.5-05, TD-M5-D4-01
- **Research:** R-BBF-09 NUEVA (WAAPI canon 2026)
- **Riesgo:** Medio (Client Component + JS interactivo)
- **Próximo:** Strategic audit → M5-F AI-readable docs

---

*B-BBF-WEB-M5-E-MOTION-SYSTEM — Motion canon BBF + WAAPI logo smooth*
*Sistema completo motion + 2 TDs latentes resueltas + 2 decisiones firmadas*