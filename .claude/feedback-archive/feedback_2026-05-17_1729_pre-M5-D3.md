# COPY_PASTE_B-BBF-WEB-M5-D3-HEROVIDEO.md

**Despacho:** B-BBF-WEB-M5-D3-HEROVIDEO
**Tipo:** Refactor M5-D3 — HeroVideo molecule compound canon
**Para:** Claude Code en VS Code
**Repo:** `/Volumes/PK/BBF/Repos/bbf-web/`
**Branch:** `feat/m5-design-system`
**HEAD pre:** `936c46d` (M5-D2 cerrado)
**Tiempo estimado:** 1.5-2h
**STOPs:** 12
**Riesgo:** Medio (preservar video hero bg + migración paths críticos)

---

## §0 — INSTRUCCIONES OPERATIVAS

### §0.1 Acciones autónomas previas

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

mkdir -p .claude/feedback-archive
[ -f feedback.md ] && mv feedback.md .claude/feedback-archive/feedback_$(date +%Y-%m-%d_%H%M)_pre-M5-D3.md

git branch --show-current
git log --oneline -3
```

### §0.2 Disciplina canon

- STOPs respetados
- Reportes literales  
- Auto-corrección autorizada (§14) si despacho referencia paths/APIs incorrectos vs código real (caso M5-D2 estableció precedente)
- NO modificar otros atoms/molecules
- NO tocar BBFLogo, LocaleSwitcher, atoms

### §0.3 Decisiones firmadas que rigen M5-D3

| ID | Aplicación |
|----|------------|
| D-BBF-WEB-77 | Surface-awareness (no relevante en HeroVideo bg) |
| D-BBF-WEB-78 | State management simple (video state via element API) |
| D-BBF-WEB-79 | **Compound components canon (SÍ aplica — HeroVideo es complejo)** |
| D-BBF-WEB-81 | Folder per component canon |
| D-BBF-WEB-82 | AI-readable (JSDoc + data-component) |
| D-BBF-WEB-83 | Atomic Design folder structure |
| D-BBF-WEB-84 | public/assets/ canon |
| **D-BBF-WEB-86** | **NUEVA — HeroVideo API compound** |
| **D-BBF-WEB-87** | **NUEVA — Migración hero assets a /assets/media/hero/** |

### §0.4 D-79 vs D-85 distinción

- **D-85 (LocaleSwitcher):** API monolítica porque pills estructuralmente iguales
- **D-86 (HeroVideo):** API compound porque Source/Overlay tienen contenidos distintos

**Esta diferencia es canon doctrinal BBF.** Aplica caso por caso.

---

## §1 — OBJETIVO

1. Refactor `src/components/HeroVideo.tsx` (flat) → `src/components/molecules/HeroVideo/` (molecule compound canon)
2. Migrar `public/media/hero/*` → `public/assets/media/hero/*`
3. Update paths en HeroVideo.tsx + verificar metadata.ts si referencia
4. Eliminar `public/media/` legacy (queda dir vacío)
5. Preservar visual hero 100%

---

## §2 — STOP 1: PRE-EXECUTION AUDIT

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "═══ Branch + HEAD ═══"
git branch --show-current
git log --oneline -3
# Esperado: 936c46d

echo ""
echo "═══ tsc clean ═══"
pnpm typecheck 2>&1 | tail -3

echo ""
echo "═══ HeroVideo.tsx actual ═══"
cat src/components/HeroVideo.tsx

echo ""
echo "═══ public/media/ contenido ═══"
ls -la public/media/ 2>/dev/null
ls -la public/media/hero/ 2>/dev/null

echo ""
echo "═══ Existe public/assets/media/hero/ ═══"
ls -la public/assets/media/hero/ 2>/dev/null

echo ""
echo "═══ Imports HeroVideo en el repo ═══"
grep -rn "from '@/components/HeroVideo'\|from '../HeroVideo'\|from './HeroVideo'" src/ 2>/dev/null

echo ""
echo "═══ Referencias /media/hero en el repo ═══"
grep -rn "/media/hero" src/ 2>/dev/null

echo ""
echo "═══ Page.tsx uso HeroVideo ═══"
grep -n "HeroVideo" src/app/\(frontend\)/\[locale\]/page.tsx
```

### §2.1 Reporte STOP 1 esperado

```
[STOP 1 completado]

Branch: feat/m5-design-system ✓
HEAD: 936c46d ✓
tsc: exit 0 ✓

Estado actual:
  - src/components/HeroVideo.tsx [X líneas]
  - public/media/hero/ contiene: [list archivos]
  - public/assets/media/hero/ existe (con README + .gitkeep)
  - Imports detectados: [lista paths]
  - Referencias /media/hero detectadas: [lista]

LISTO para STOP 2.
```

### §2.2 Bloqueo

Si HeroVideo.tsx tiene estructura inesperada (ej. usa hooks específicos, tiene logica de auto-pause, etc), REPORTAR antes de refactor.

---

## §3 — STOP 2: MIGRAR ASSETS HERO

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

# Verificar destino existe
ls -la public/assets/media/hero/

# Mover archivos de media/hero/ a assets/media/hero/
# (preservar nombres exactos)
for file in public/media/hero/*; do
  if [ -f "$file" ]; then
    filename=$(basename "$file")
    git mv "$file" "public/assets/media/hero/$filename"
    echo "Movido: $filename"
  fi
done

# Verificar resultado
ls -la public/assets/media/hero/

# Eliminar dir legacy si quedó vacío
if [ -d "public/media/hero" ] && [ -z "$(ls -A public/media/hero)" ]; then
  rmdir public/media/hero
  echo "✓ public/media/hero eliminado"
fi

# Si public/media/ queda vacío también
if [ -d "public/media" ] && [ -z "$(ls -A public/media)" ]; then
  rmdir public/media
  echo "✓ public/media eliminado"
fi

ls -la public/media/ 2>&1 || echo "public/media/ ya no existe ✓"
```

### §3.1 Reporte STOP 2

```
[STOP 2 completado]

Archivos migrados:
  ✓ hero.av1.webm → public/assets/media/hero/
  ✓ hero.h264.mp4 → public/assets/media/hero/
  ✓ [otros si los hubo]

Dirs eliminados:
  ✓ public/media/hero (vacío)
  ✓ public/media (vacío)

LISTO para STOP 3.
```

---

## §4 — STOP 3: CREAR ESTRUCTURA molecule

```bash
mkdir -p src/components/molecules/HeroVideo
ls -la src/components/molecules/
```

---

## §5 — STOP 4: CREAR HeroVideo.variants.ts

```typescript
// src/components/molecules/HeroVideo/HeroVideo.variants.ts
```

**Contenido exacto:**

```typescript
/**
 * BBF Design System — HeroVideo molecule variants
 *
 * Subordinado a: B-BBF-WEB-M5-D3-HEROVIDEO
 * Decisiones: D-78 (state simple), D-82 (AI-readable), D-86 (compound API)
 */

import { cva, type VariantProps } from 'class-variance-authority';

/**
 * Root container variants
 */
export const heroVideoVariants = cva(
  ['bbf-hero-video', 'absolute', 'inset-0', 'overflow-hidden'],
  {
    variants: {
      fit: {
        cover: '[--bbf-hero-video-object-fit:cover]',
        contain: '[--bbf-hero-video-object-fit:contain]',
        fill: '[--bbf-hero-video-object-fit:fill]',
      },
    },
    defaultVariants: {
      fit: 'cover',
    },
  }
);

/**
 * Overlay variants
 */
export const heroVideoOverlayVariants = cva(
  ['bbf-hero-video__overlay', 'absolute', 'inset-0', 'pointer-events-none'],
  {
    variants: {
      tone: {
        none: '',
        dark: 'bg-gradient-to-b from-black/0 via-black/20 to-black/40',
        light: 'bg-gradient-to-b from-white/0 via-white/20 to-white/40',
        custom: '', // usa --bbf-hero-overlay-color
      },
    },
    defaultVariants: {
      tone: 'none',
    },
  }
);

export type HeroVideoVariants = VariantProps<typeof heroVideoVariants>;
export type HeroVideoOverlayVariants = VariantProps<typeof heroVideoOverlayVariants>;
```

---

## §6 — STOP 5: CREAR HeroVideo.tsx (compound canon)

```typescript
// src/components/molecules/HeroVideo/HeroVideo.tsx
```

**Contenido exacto:**

```typescript
/**
 * BBF Design System — HeroVideo molecule
 *
 * Subordinado a: B-BBF-WEB-M5-D3-HEROVIDEO
 * Decisiones: D-79 (compound), D-82 (AI-readable), D-86 (compound API),
 *             D-87 (assets canon paths)
 *
 * API compound canon BBF (D-79 aplica — Source/Overlay contenidos distintos):
 *
 *   <HeroVideo poster="..." autoplay muted loop>
 *     <HeroVideo.Source src="..." type="webm-vp9" />
 *     <HeroVideo.Source src="..." type="mp4-h264" />
 *     <HeroVideo.Overlay tone="none" />
 *   </HeroVideo>
 *
 * Server Component (sin state interactivo, atributos native HTML video).
 */

import type { ReactNode } from 'react';
import { cn } from '@/lib/utils';
import {
  heroVideoVariants,
  heroVideoOverlayVariants,
  type HeroVideoVariants,
  type HeroVideoOverlayVariants,
} from './HeroVideo.variants';

/**
 * Source type → MIME type canon BBF.
 * Strategic mapea string simple a MIME completo para limpieza API.
 */
const SOURCE_TYPE_MAP = {
  'webm-vp9':  'video/webm; codecs="vp9"',
  'webm-av1':  'video/webm; codecs="av01.0.05M.08"',
  'mp4-h264':  'video/mp4; codecs="avc1.4D401E,mp4a.40.2"',
  'mp4-h265':  'video/mp4; codecs="hvc1"',
  'mp4-av1':   'video/mp4; codecs="av01.0.05M.08,mp4a.40.2"',
  'mov':       'video/quicktime',
} as const;

export type HeroVideoSourceType = keyof typeof SOURCE_TYPE_MAP;

/* ============================================================
   ROOT
   ============================================================ */
export interface HeroVideoProps extends HeroVideoVariants {
  /**
   * Poster frame image (preview antes de cargar video).
   * Path canon: /assets/media/hero/hero-poster.{jpg|webp}
   */
  poster?: string;
  /**
   * Autoplay (requiere muted=true en browsers modernos).
   */
  autoplay?: boolean;
  muted?: boolean;
  loop?: boolean;
  playsInline?: boolean;
  /**
   * Preload strategy. Default: 'metadata' (canon performance).
   */
  preload?: 'none' | 'metadata' | 'auto';
  /**
   * ARIA label opcional. Default: aria-hidden=true (video decorativo).
   */
  ariaLabel?: string;
  className?: string;
  children: ReactNode;
}

/**
 * BBF HeroVideo molecule — Atomic Design canon
 *
 * @description
 * Video hero canon BBF. Compound components dot notation (D-79).
 * Sources tipados con MIME map. Overlay opcional con tone variant.
 *
 * @example Default (sin overlay)
 * ```tsx
 * <HeroVideo poster="/assets/media/hero/hero-poster.jpg" autoplay muted loop>
 *   <HeroVideo.Source src="/assets/media/hero/hero.av1.webm" type="webm-vp9" />
 *   <HeroVideo.Source src="/assets/media/hero/hero.h264.mp4" type="mp4-h264" />
 * </HeroVideo>
 * ```
 *
 * @example Con overlay dark
 * ```tsx
 * <HeroVideo poster="..." autoplay muted loop>
 *   <HeroVideo.Source src="..." type="webm-vp9" />
 *   <HeroVideo.Source src="..." type="mp4-h264" />
 *   <HeroVideo.Overlay tone="dark" />
 * </HeroVideo>
 * ```
 *
 * @example Fit contain (no fill)
 * ```tsx
 * <HeroVideo fit="contain" poster="...">...</HeroVideo>
 * ```
 */
function HeroVideoRoot({
  poster,
  autoplay = false,
  muted = false,
  loop = false,
  playsInline = true,
  preload = 'metadata',
  fit,
  ariaLabel,
  className,
  children,
}: HeroVideoProps) {
  return (
    <div
      data-component="bbf-hero-video"
      className={cn(heroVideoVariants({ fit }), className)}
    >
      <video
        autoPlay={autoplay}
        muted={muted}
        loop={loop}
        playsInline={playsInline}
        preload={preload}
        poster={poster}
        aria-label={ariaLabel}
        aria-hidden={ariaLabel ? undefined : true}
        className="absolute inset-0 h-full w-full"
        style={{ objectFit: 'var(--bbf-hero-video-object-fit, cover)' as 'cover' }}
      >
        {children}
      </video>
    </div>
  );
}

/* ============================================================
   SOURCE (sub-component)
   ============================================================ */
export interface HeroVideoSourceProps {
  src: string;
  type: HeroVideoSourceType;
}

/**
 * HeroVideo.Source — sub-component canon
 *
 * @example
 * ```tsx
 * <HeroVideo.Source src="/assets/media/hero/hero.av1.webm" type="webm-vp9" />
 * ```
 */
function HeroVideoSource({ src, type }: HeroVideoSourceProps) {
  const mimeType = SOURCE_TYPE_MAP[type];
  return (
    <source
      data-component="bbf-hero-video-source"
      data-source-type={type}
      src={src}
      type={mimeType}
    />
  );
}

/* ============================================================
   OVERLAY (sub-component opcional)
   ============================================================ */
export interface HeroVideoOverlayProps extends HeroVideoOverlayVariants {
  /**
   * Opacity override 0..1. Default según tone variant.
   */
  opacity?: number;
  /**
   * Color custom CSS (solo si tone='custom').
   */
  color?: string;
  className?: string;
}

/**
 * HeroVideo.Overlay — sub-component opcional canon
 *
 * @example
 * ```tsx
 * <HeroVideo.Overlay tone="dark" />
 * <HeroVideo.Overlay tone="custom" color="rgba(0,0,0,0.3)" />
 * ```
 */
function HeroVideoOverlay({
  tone = 'none',
  opacity,
  color,
  className,
}: HeroVideoOverlayProps) {
  if (tone === 'none' && !opacity && !color) return null;

  const style: React.CSSProperties = {};
  if (opacity !== undefined) style.opacity = opacity;
  if (tone === 'custom' && color) style.backgroundColor = color;

  return (
    <div
      data-component="bbf-hero-video-overlay"
      data-tone={tone}
      className={cn(heroVideoOverlayVariants({ tone }), className)}
      style={Object.keys(style).length > 0 ? style : undefined}
      aria-hidden="true"
    />
  );
}

/* ============================================================
   COMPOUND EXPORT (canon Radix pattern)
   ============================================================ */
export const HeroVideo = Object.assign(HeroVideoRoot, {
  Source: HeroVideoSource,
  Overlay: HeroVideoOverlay,
});
```

---

## §7 — STOP 6: CREAR index.ts barrel

```typescript
// src/components/molecules/HeroVideo/index.ts
```

**Contenido exacto:**

```typescript
export { HeroVideo } from './HeroVideo';
export type {
  HeroVideoProps,
  HeroVideoSourceProps,
  HeroVideoOverlayProps,
  HeroVideoSourceType,
} from './HeroVideo';
export {
  heroVideoVariants,
  heroVideoOverlayVariants,
} from './HeroVideo.variants';
export type {
  HeroVideoVariants,
  HeroVideoOverlayVariants,
} from './HeroVideo.variants';
```

---

## §8 — STOP 7: UPDATE molecules/index.ts

```typescript
// src/components/molecules/index.ts
```

Update barrel para incluir HeroVideo:

```typescript
/**
 * BBF Design System — Molecules barrel export
 *
 * Subordinado a: BBF_M5_D_Plan.md
 * Decisión: D-BBF-WEB-83 (Atomic Design folder structure)
 */

export * from './LocaleSwitcher';
export * from './HeroVideo';
```

---

## §9 — STOP 8: UPDATE page.tsx CON COMPOUND API

```bash
# Audit uso actual
grep -n "HeroVideo\|/media/hero" src/app/\(frontend\)/\[locale\]/page.tsx
```

**Cambios requeridos:**

1. **Import:**
```tsx
// ANTES
import { HeroVideo } from '@/components/HeroVideo';

// DESPUÉS
import { HeroVideo } from '@/components/molecules/HeroVideo';
```

2. **JSX uso** (refactor a compound):

```tsx
// ANTES (monolítico legacy)
<HeroVideo />
// O equivalente con <video><source>... inline en page.tsx

// DESPUÉS (compound canon, paths nuevos)
<HeroVideo
  poster="/assets/media/hero/hero-poster.jpg"
  autoplay
  muted
  loop
>
  <HeroVideo.Source src="/assets/media/hero/hero.av1.webm" type="webm-vp9" />
  <HeroVideo.Source src="/assets/media/hero/hero.h264.mp4" type="mp4-h264" />
</HeroVideo>
```

**NOTA AUDIT:** El audit STOP 1 te dirá si el video bg está en `<HeroVideo />` o si está inline en page.tsx. Si es inline en page.tsx (como sugiere HTML vercel preview), **migrar el `<video>` actual a `<HeroVideo>` compound** preservando atributos exactos.

**Si no existe hero-poster.jpg:** comentar la prop `poster` (Strategic registra TD-M5-D3-01 para crear poster en M6+).

---

## §10 — STOP 9: ELIMINAR FLAT LEGACY

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

# Audit que no quede ningún import legacy
grep -rn "from '@/components/HeroVideo'" src/ 2>/dev/null

# Si retorna vacío, eliminar legacy
if [ -f "src/components/HeroVideo.tsx" ]; then
  git rm src/components/HeroVideo.tsx
fi
```

---

## §11 — STOP 10: BUILD + DEV SMOKE TEST

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

pnpm typecheck 2>&1 | tail -3
echo "tsc exit: $?"

echo "N" | pnpm build 2>&1 | tail -15
echo "build exit: $?"
```

### §11.1 Dev smoke test

```bash
pnpm dev > /tmp/bbf-dev.log 2>&1 &
DEV_PID=$!

until curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ 2>/dev/null | grep -q 200; do
  sleep 2
done

echo "═══ HTTP STATUS ═══"
echo "/ ES:   $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/)"
echo "/en EN: $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/)"

echo ""
echo "═══ HeroVideo HTML renderizado ═══"
curl -s http://localhost:3000/ | grep -oE 'data-component="bbf-hero-video"[^>]*' | head -1
curl -s http://localhost:3000/ | grep -oE 'data-component="bbf-hero-video-source"[^>]*' | head -3
curl -s http://localhost:3000/ | grep -oE 'data-source-type="[^"]*"' | head -3

echo ""
echo "═══ Source paths canon ═══"
curl -s http://localhost:3000/ | grep -oE 'src="[^"]*hero\.[^"]*"' | head -3

echo ""
echo "═══ Video element attributes ═══"
curl -s http://localhost:3000/ | grep -oE '<video[^>]*>' | head -1

kill $DEV_PID 2>/dev/null
wait 2>/dev/null
```

### §11.2 Verificaciones críticas

| Test | Esperado |
|------|----------|
| Build exit 0 | ✅ |
| 5/5 páginas estáticas | ✅ |
| / 200 + /en 200 | ✅ |
| `data-component="bbf-hero-video"` presente | ✅ |
| `data-component="bbf-hero-video-source"` (2 instancias) | ✅ |
| `data-source-type="webm-vp9"` y `"mp4-h264"` presentes | ✅ |
| Paths `/assets/media/hero/hero.*` (NO `/media/hero/`) | ✅ |
| `<video>` con autoplay muted loop playsinline | ✅ |

### §11.3 Bloqueo

Si video bg NO carga visualmente, paths incorrectos. REPORTAR Y DETENER.

---

## §12 — STOP 11: COMMIT + PUSH

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

git status --short

git add public/assets/media/hero/
git add src/components/molecules/
git add src/app/\(frontend\)/\[locale\]/page.tsx

# Si git mv funcionó correctamente, los archivos viejos ya están en staging
git status --short

git commit -m "feat(m5-d3): HeroVideo molecule compound canon + assets migration

B-BBF-WEB-M5-D3-HEROVIDEO

Refactor HeroVideo flat → molecule compound canon BBF.
Migración assets hero a public/assets/media/hero/ canon.

Decisiones firmadas NUEVAS:
- D-BBF-WEB-86: HeroVideo API compound (D-79 aplica)
  Razón: Sources y Overlay tienen contenidos estructuralmente
  distintos. A diferencia de LocaleSwitcher (D-85 monolítica),
  HeroVideo justifica compound real.

- D-BBF-WEB-87: Migración hero assets a /assets/media/hero/
  Razón: D-84 estableció estructura canon. Mantener legacy
  /media/hero/ creaba deuda doctrinal de 2 paths vivos.

Decisiones aplicadas:
- D-79: Compound components dot notation (Source + Overlay)
- D-81: Folder per component canon
- D-82: AI-readable (JSDoc + data-component + data-source-type)
- D-83: Atomic Design folder structure
- D-84: public/assets/ structure canon

Cambios:

Migración assets (D-87):
- public/media/hero/hero.av1.webm → public/assets/media/hero/
- public/media/hero/hero.h264.mp4 → public/assets/media/hero/
- public/media/hero/ eliminado (vacío)
- public/media/ eliminado (vacío)

Molecule compound canon:
- src/components/molecules/HeroVideo/
  ├── HeroVideo.tsx (compound Root + Source + Overlay)
  ├── HeroVideo.variants.ts (CVA fit + overlay tone)
  └── index.ts
- src/components/molecules/index.ts actualizado

Atom HeroVideo refactor (D-86):
- API compound: <HeroVideo><HeroVideo.Source /><HeroVideo.Overlay /></HeroVideo>
- Server Component (sin state interactivo)
- Object.assign pattern canon Radix
- Sources tipados con MIME map (webm-vp9, mp4-h264, etc)
- Overlay opcional con tone variant (none/dark/light/custom)
- Fit variant (cover/contain/fill)
- data-component, data-source-type, data-tone attributes
- JSDoc con 3 @example casos

Eliminados:
- src/components/HeroVideo.tsx (migrado)

Page.tsx:
- Import: @/components/HeroVideo → @/components/molecules/HeroVideo
- JSX: refactor a compound + paths /assets/media/hero/

TDs resueltas:
- TD-M5-D1.5-03: Video hero NO migrado a assets/ (resuelta)

Verificación:
- tsc --noEmit: exit 0
- pnpm build: exit 0 (5/5 páginas)
- Video bg funcional visualmente preservado
- Paths /assets/media/hero/ confirmados en HTML

Refs: B-BBF-WEB-M5-D3-HEROVIDEO
Refs: BBF_M5_D_Plan.md §4 (M5-D3)"

git push origin feat/m5-design-system

git log --oneline -6
```

---

## §13 — REPORTE FINAL ESPERADO

```
═══ M5-D3 COMPLETADO ═══

Branch: feat/m5-design-system
Commit: <hash>
Push: ✓ origin

HeroVideo molecule compound canon:
  ✓ src/components/molecules/HeroVideo/
  ✓ API compound D-86: <HeroVideo><HeroVideo.Source /><HeroVideo.Overlay /></HeroVideo>
  ✓ Object.assign pattern canon Radix
  ✓ Sources tipados con MIME map
  ✓ Overlay opcional tone variant
  ✓ data-component + data-source-type + data-tone attributes

Migración assets (D-87):
  ✓ hero.av1.webm + hero.h264.mp4 → public/assets/media/hero/
  ✓ public/media/ eliminado (TD-M5-D1.5-03 resuelta)

Eliminados:
  ✓ src/components/HeroVideo.tsx (migrado)

Verificación:
  ✓ tsc + build exit 0 (5/5 páginas)
  ✓ Video bg visual idéntico
  ✓ Paths /assets/media/hero/ en HTML
  ✓ data-attributes presentes

LISTO STRATEGIC VISUAL REVIEW VERCEL.

Checklist:
  - Video hero bg funcional ✓
  - Loop + autoplay + muted ✓
  - Logo + LocaleSwitcher + Headline preservados ✓
  - 0 console errors (especialmente 404 paths) ✓

Si OK → autorizar M5-D4 (HeroSection).
```

---

## §14 — ESCALATION RULES

**Detener y reportar si:**
- Video bg NO carga (404 en paths nuevos)
- TypeScript error en Object.assign pattern (raro pero posible)
- Build falla por path imports
- Visual hero cambia perceptiblemente (overlay, fit, sizing)

**Auto-corrección autorizada (precedente M5-D2):**
- Import paths reales vs despacho inferido
- Naming convention de archivos
- API React/Next.js versions específicas
- Si HeroVideo.tsx tenía lógica especial (ej. lazy load, intersection observer), preservar en refactor

**REPORTAR autocorrección al final:**
Si aplicaste auto-correcciones, reportar en commit message + final summary.

---

## §15 — METADATA

- **Despacho:** B-BBF-WEB-M5-D3-HEROVIDEO
- **Versión:** v1.0
- **Fecha:** 2026-05-17
- **HEAD pre:** `936c46d`
- **STOPs:** 12
- **Decisiones aplicadas:** D-77, D-78, D-79, D-81, D-82, D-83, D-84, **D-86 NUEVA**, **D-87 NUEVA**
- **TDs resueltas:** TD-M5-D1.5-03 (hero assets migration)
- **TDs heredadas:** TD-M5-D6 cleanup pending
- **Riesgo:** Medio (preservar visual hero + migración paths)
- **Próximo:** Strategic audit → M5-D4 HeroSection

---

*B-BBF-WEB-M5-D3-HEROVIDEO — Molecule compound canon*
*Sources tipados + Overlay opcional + Assets migration canon*