# COPY_PASTE_B-BBF-WEB-M5-D5-SHADOW-TOKENS.md

**Despacho:** B-BBF-WEB-M5-D5-SHADOW-TOKENS
**Tipo:** Refactor minor — Shadow tokens canon BBF
**Para:** Claude Code en VS Code
**Repo:** `/Volumes/PK/BBF/Repos/bbf-web/`
**Branch:** `feat/m5-design-system`
**HEAD pre:** `f183522` (M5-D4 cerrado)
**Tiempo estimado:** 1h - 1.5h
**STOPs:** 9
**Riesgo:** Bajo (cleanup técnico, visual preservado)

---

## §0 — INSTRUCCIONES OPERATIVAS

### §0.1 Acciones autónomas previas

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

mkdir -p .claude/feedback-archive
[ -f feedback.md ] && mv feedback.md .claude/feedback-archive/feedback_$(date +%Y-%m-%d_%H%M)_pre-M5-D5.md

git branch --show-current
git log --oneline -3
```

### §0.2 Disciplina canon

- STOPs respetados
- Reportes literales
- Auto-corrección §14 autorizada
- **Visual debe permanecer idéntico** (cleanup técnico, no visual change)

### §0.3 Decisiones firmadas que rigen M5-D5

| ID | Aplicación |
|----|------------|
| D-69 | OKLCH paleta canon BBF (M5-B) |
| D-82 | AI-readable (comentarios canon) |
| **D-93 NUEVA** | **Shadow tokens semantic canon BBF** |

---

## §1 — OBJETIVO

1. Audit shadows hardcoded (rgb/rgba) en codebase
2. Crear sistema canon de shadow tokens semantic (sm, md, lg, xl)
3. Migrar todas las shadows a tokens canon
4. Visual idéntico (preservación 100%)
5. Documentar pattern canon Tailwind v4 shadow

---

## §2 — STOP 1: AUDIT SHADOWS HARDCODED

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "═══ Branch + HEAD ═══"
git branch --show-current
git log --oneline -3
# Esperado: f183522

echo ""
echo "═══ tsc clean ═══"
pnpm typecheck 2>&1 | tail -3

echo ""
echo "═══ 1. Audit completo: box-shadow + rgba/rgb hardcoded ═══"
grep -rn "box-shadow.*rgba\|box-shadow.*rgb(" src/styles/ 2>/dev/null

echo ""
echo "═══ 2. Audit en globals.css ═══"
grep -n "box-shadow\|rgba\|rgb(" src/app/globals.css 2>/dev/null | head -10

echo ""
echo "═══ 3. Tokens shadow existentes (verificar si hay base canon) ═══"
grep -rn "bbf-shadow\|--bbf.*shadow" src/styles/ 2>/dev/null

echo ""
echo "═══ 4. Listado completo archivos CSS components ═══"
ls -la src/styles/tokens/components/

echo ""
echo "═══ 5. semantic tokens dir ═══"
ls -la src/styles/tokens/semantic/

echo ""
echo "═══ 6. primitives tokens dir ═══"
ls -la src/styles/tokens/primitives/
```

### §2.1 Reporte STOP 1 esperado

```
[STOP 1 completado]

Branch: feat/m5-design-system ✓
HEAD: f183522 ✓
tsc: exit 0 ✓

Shadows hardcoded detectadas:
  - hero.css:LX  → rgba(0, 0, 0, 0.06)
  - locale-switcher.css:LX  → rgba(0, 0, 0, 0.06)
  - [otros si hay]

Tokens shadow existentes: [si hay alguno preservar mapeo]

Archivos potencialmente afectados:
  - src/styles/tokens/components/[N archivos]

LISTO para STOP 2.
```

### §2.2 Bloqueo

Si hay 0 shadows hardcoded (improbable), REPORTAR. M5-D5 trivial entonces.
Si hay 20+ shadows (más de lo esperado), REPORTAR para evaluar scope expansion.

---

## §3 — STOP 2: PROPUESTA TOKENS CANON

Antes de implementar, Strategic propone el sistema de shadow tokens canon BBF:

### §3.1 Sistema canon propuesto

```css
/* Sistema canon: 5 niveles de elevación
 * Naming canon BBF: sm, md, lg, xl, 2xl
 * Color base: OKLCH (alineado D-69)
 * Pattern: --bbf-shadow-{size}
 */

--bbf-shadow-xs: 0 1px 2px 0 oklch(0 0 0 / 0.04);
--bbf-shadow-sm: 0 2px 8px 0 oklch(0 0 0 / 0.06);   /* TD-M5-B-01 actual */
--bbf-shadow-md: 0 4px 16px 0 oklch(0 0 0 / 0.10);
--bbf-shadow-lg: 0 8px 30px 0 oklch(0 0 0 / 0.15);
--bbf-shadow-xl: 0 16px 48px 0 oklch(0 0 0 / 0.20);
```

### §3.2 Mapping legacy → canon

| Legacy hardcoded | Token canon |
|------------------|-------------|
| `rgba(0,0,0,0.04)` 1px | `--bbf-shadow-xs` |
| `rgba(0,0,0,0.06)` 2px 8px | `--bbf-shadow-sm` |
| `rgba(0,0,0,0.10)` 4px 16px | `--bbf-shadow-md` |
| `rgba(0,0,0,0.15)` 8px 30px | `--bbf-shadow-lg` |
| `rgba(0,0,0,0.20)` 16px 48px | `--bbf-shadow-xl` |

**Decisión doctrinal:** El audit STOP 1 dirá QUÉ shadows reales existen. Strategic ajusta la propuesta si hay variaciones inesperadas.

---

## §4 — STOP 3: CREAR semantic/shadows.css

```bash
# Verificar si existe
ls -la src/styles/tokens/semantic/shadows.css 2>/dev/null
```

Si NO existe, crear:

```css
/* src/styles/tokens/semantic/shadows.css */
```

**Contenido exacto:**

```css
/**
 * BBF Design System — SEMANTIC TOKEN: Shadows
 *
 * Subordinado a: B-BBF-WEB-M5-D5-SHADOW-TOKENS
 * Decisiones: D-69 (OKLCH paleta), D-93 (shadow tokens semantic)
 *
 * Sistema canon de elevación BBF: 5 niveles de profundidad.
 * Color base OKLCH alineado con paleta canon (D-69).
 *
 * Pattern canon Tailwind v4:
 *   - Usar var(--bbf-shadow-{size}) directamente en CSS
 *   - En Tailwind: [box-shadow:var(--bbf-shadow-sm)] arbitrary property
 *
 * Tokens:
 *   --bbf-shadow-xs   subtle (1px 2px / 0.04 alpha)
 *   --bbf-shadow-sm   default (2px 8px / 0.06 alpha)
 *   --bbf-shadow-md   elevated (4px 16px / 0.10 alpha)
 *   --bbf-shadow-lg   floating (8px 30px / 0.15 alpha)
 *   --bbf-shadow-xl   modal (16px 48px / 0.20 alpha)
 */

:root {
  /* ============================================================
     ELEVATION SCALE (5 niveles canon BBF)
     ============================================================ */
  --bbf-shadow-xs: 0 1px 2px 0 oklch(0 0 0 / 0.04);
  --bbf-shadow-sm: 0 2px 8px 0 oklch(0 0 0 / 0.06);
  --bbf-shadow-md: 0 4px 16px 0 oklch(0 0 0 / 0.10);
  --bbf-shadow-lg: 0 8px 30px 0 oklch(0 0 0 / 0.15);
  --bbf-shadow-xl: 0 16px 48px 0 oklch(0 0 0 / 0.20);

  /* ============================================================
     SEMANTIC ALIASES (uso contextual)
     ============================================================ */
  --bbf-shadow-card: var(--bbf-shadow-sm);
  --bbf-shadow-floating: var(--bbf-shadow-lg);
  --bbf-shadow-modal: var(--bbf-shadow-xl);
  --bbf-shadow-button-hover: var(--bbf-shadow-md);
}
```

---

## §5 — STOP 4: IMPORT shadows.css EN GLOBALS

```bash
# Audit imports actuales
grep -n "@import" src/app/globals.css
```

Agregar import en orden canon (después de colors semantic, antes de components):

```css
/* En globals.css buscar bloque @import semantic y agregar: */
@import '../styles/tokens/semantic/shadows.css';
```

**Orden canon expected:**
1. Reset
2. Primitives
3. Semantic (colors → typography → spacing → **shadows NUEVO**)
4. Components
5. Utilities

---

## §6 — STOP 5: MIGRAR shadows en components CSS

Migrar **cada archivo CSS de components** que el audit STOP 1 detectó.

### §6.1 hero.css (TD-M5-B-01 confirmado)

```bash
# Audit context
grep -B1 -A1 "rgba\|rgb(" src/styles/tokens/components/hero.css
```

**Strategy:** Reemplazar TODA línea `box-shadow: ... rgba(...)` por `box-shadow: ... var(--bbf-shadow-{size})`.

**EJEMPLO de cambio:**

```css
/* ANTES */
.bbf-cta-pill {
  ...
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

/* DESPUÉS */
.bbf-cta-pill {
  ...
  box-shadow: var(--bbf-shadow-sm);
}
```

**Estrategia exacta:** El valor `0 2px 8px rgba(0, 0, 0, 0.06)` = `--bbf-shadow-sm`. Reemplazar con token canon.

### §6.2 locale-switcher.css

```bash
grep -n "box-shadow\|rgba" src/styles/tokens/components/locale-switcher.css
```

Strategic emitió en M5-D2:
```css
.bbf-locale-switcher {
  ...
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);  ← TD-M5-B-01 latente
}
```

Reemplazar por:
```css
.bbf-locale-switcher {
  ...
  box-shadow: var(--bbf-shadow-sm);
}
```

### §6.3 Otros components

Para cada otro CSS file con shadows hardcoded detectado en STOP 1:
- Identificar valor → mapear a token canon
- Reemplazar

---

## §7 — STOP 6: AUDIT POST-MIGRACIÓN

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

echo "═══ 1. Confirmar shadows hardcoded eliminadas ═══"
grep -rn "box-shadow.*rgba\|box-shadow.*rgb(" src/styles/ 2>/dev/null
echo "  Esperado: 0 resultados"

echo ""
echo "═══ 2. Confirmar tokens shadow en uso ═══"
grep -rn "var(--bbf-shadow" src/styles/tokens/components/ 2>/dev/null

echo ""
echo "═══ 3. Confirmar shadows.css existe + correcto ═══"
ls -la src/styles/tokens/semantic/shadows.css
echo ""
head -30 src/styles/tokens/semantic/shadows.css

echo ""
echo "═══ 4. Confirmar import en globals.css ═══"
grep -n "shadows.css" src/app/globals.css
```

### §7.1 Verificaciones críticas

| Test | Esperado |
|------|----------|
| `grep box-shadow.*rgba` | 0 resultados |
| Tokens canon en uso | ≥2 archivos |
| shadows.css existe | ✅ |
| Import globals correcto | ✅ |

---

## §8 — STOP 7: BUILD + DEV SMOKE TEST

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

pnpm typecheck 2>&1 | tail -3
echo "tsc exit: $?"

echo "N" | pnpm build 2>&1 | tail -10
echo "build exit: $?"
```

### §8.1 Dev smoke test

```bash
pnpm dev > /tmp/bbf-dev.log 2>&1 &
DEV_PID=$!

until curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ 2>/dev/null | grep -q 200; do
  sleep 2
done

echo "═══ HTTP STATUS ═══"
echo "/ ES:   $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/)"
echo "/en EN: $(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/en)"

echo ""
echo "═══ CSS bundle — verificar shadow tokens compilados ═══"
grep -o "bbf-shadow-sm:[^;]*" .next/static/css/*.css 2>/dev/null | head -2
echo ""
echo "--- bbf-shadow-md ---"
grep -o "bbf-shadow-md:[^;]*" .next/static/css/*.css 2>/dev/null | head -2

echo ""
echo "═══ CSS bundle — verificar var(--bbf-shadow-) usado ═══"
grep -o "var(--bbf-shadow-[^)]*)" .next/static/css/*.css 2>/dev/null | head -5

kill $DEV_PID 2>/dev/null
wait 2>/dev/null
```

### §8.2 Bloqueo

Si build falla por TypeScript errors, **NO debería pasar** (M5-D5 es solo CSS). REPORTAR si pasa.

---

## §9 — STOP 8: COMMIT + PUSH

```bash
cd /Volumes/PK/BBF/Repos/bbf-web

git status --short

git add src/styles/tokens/semantic/shadows.css
git add src/styles/tokens/components/
git add src/app/globals.css

git status --short

git commit -m "feat(m5-d5): shadow tokens canon BBF — semantic system OKLCH

B-BBF-WEB-M5-D5-SHADOW-TOKENS

Sistema canon de shadow tokens semantic BBF.
Resuelve TD-M5-B-01 (rgb shadow alpha hardcoded).

Decisión firmada NUEVA:
- D-BBF-WEB-93: Shadow tokens semantic system canon BBF.
  5 niveles de elevación (xs, sm, md, lg, xl) OKLCH alineado D-69.
  Aliases semánticos contextuales (card, floating, modal, button-hover).
  Pattern Tailwind v4: var(--bbf-shadow-) en CSS,
  [box-shadow:var(--bbf-shadow-)] arbitrary property en JSX.

Cambios:

Sistema canon nuevo:
- src/styles/tokens/semantic/shadows.css
  ├── 5 niveles elevación primitivos (xs, sm, md, lg, xl)
  └── 4 aliases semánticos (card, floating, modal, button-hover)

Migraciones (rgba → tokens canon):
- hero.css: rgba(0,0,0,0.06) → var(--bbf-shadow-sm)
- locale-switcher.css: rgba(0,0,0,0.06) → var(--bbf-shadow-sm)
- [otros migrated según audit STOP 1]

globals.css:
- @import shadows.css agregado en orden canon
  (después de colors/typography/spacing semantic,
   antes de components)

TDs resueltas:
- TD-M5-B-01: rgb shadow alpha en hero.css → tokens canon

Verificación:
- 0 box-shadow:rgba(...) remanentes en src/styles/
- tsc + build exit 0
- CSS bundle: bbf-shadow-{xs,sm,md,lg,xl} compilados
- Visual idéntico (mismos valores numéricos, refactor a tokens)

Refs: B-BBF-WEB-M5-D5-SHADOW-TOKENS
Refs: D-BBF-WEB-93 (shadow tokens canon)"

git push origin feat/m5-design-system
git log --oneline -3
```

---

## §10 — REPORTE FINAL ESPERADO

```
═══ M5-D5 COMPLETADO ═══

Branch: feat/m5-design-system
Commit: <hash>
Push: ✓ origin

Sistema canon shadows:
  ✓ src/styles/tokens/semantic/shadows.css (5 niveles + 4 aliases)
  ✓ OKLCH alineado D-69
  ✓ Pattern canon documentado en header

Migraciones:
  ✓ hero.css → var(--bbf-shadow-sm)
  ✓ locale-switcher.css → var(--bbf-shadow-sm)
  ✓ [N otros archivos según audit]

globals.css:
  ✓ @import shadows.css en orden canon

TDs resueltas:
  ✓ TD-M5-B-01: rgb shadow alpha

Verificación:
  ✓ tsc + build exit 0 (5/5 páginas)
  ✓ 0 rgba shadows hardcoded en src/styles/
  ✓ Tokens canon compilados en bundle
  ✓ Visual idéntico (sin regresión esperada)

LISTO STRATEGIC VERIFICACIÓN VISUAL VERCEL.

Checklist visual (debe ser idéntico):
  - LocaleSwitcher shadow preservada ✓
  - CTA pill shadow preservada ✓
  - Hero visual sin cambios ✓
  - 0 console errors ✓

Si OK → autorizar M5-D6 (cleanup + ratificaciones).
```

---

## §11 — ESCALATION RULES

**Detener y reportar si:**
- Visual cambia perceptiblemente (shadow más fuerte/débil de lo esperado)
- OKLCH alpha rendering inconsistente entre browsers
- Build falla
- Más de 10 archivos afectados (scope mayor del esperado)

**Auto-corrección §14 autorizada:**
- Naming canon (xs vs xxs, etc.)
- Aliases semánticos (si hero-section.css usa shadow distinto, agregar alias)
- Orden imports en globals.css

---

## §12 — METADATA

- **Despacho:** B-BBF-WEB-M5-D5-SHADOW-TOKENS
- **Versión:** v1.0
- **Fecha:** 2026-05-17
- **HEAD pre:** `f183522`
- **STOPs:** 9
- **Decisiones aplicadas:** D-69 (OKLCH), D-82 (AI-readable), **D-93 NUEVA**
- **TDs resueltas:** TD-M5-B-01
- **TDs heredadas:** sin cambios
- **Riesgo:** Bajo (cleanup técnico, visual preservado)
- **Próximo:** Strategic audit → M5-D6 cleanup + ratificaciones

---

*B-BBF-WEB-M5-D5-SHADOW-TOKENS — Shadow tokens canon BBF OKLCH semantic*
*Sistema 5 niveles elevación + aliases contextuales*